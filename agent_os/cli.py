from __future__ import annotations

import argparse
import json
from pathlib import Path

from agent_os.budget_trust import (
    format_risk_counts,
    write_budget_trust_posture_report,
)
from agent_os.capability_expansion import (
    render_capability_expansion_ledger_line,
    write_capability_expansion_ledger,
)
from agent_os.capability_approval_boundary import (
    format_recommended_commands as format_approval_boundary_commands,
    render_capability_approval_boundary_matrix_line,
    write_capability_approval_boundary_matrix,
)
from agent_os.capability_evidence_collection import (
    format_recommended_commands as format_evidence_collection_commands,
    render_capability_evidence_collection_plan_line,
    write_capability_evidence_collection_plan,
)
from agent_os.capability_promotion_gate import (
    format_recommended_commands as format_promotion_gate_commands,
    render_capability_promotion_gate_checklist_line,
    write_capability_promotion_gate_checklist,
)
from agent_os.capability_promotion_decision import (
    format_recommended_commands as format_promotion_decision_commands,
    render_capability_promotion_decision_ledger_line,
    write_capability_promotion_decision_ledger,
)
from agent_os.capability_trust_promotion import (
    format_recommended_commands as format_trust_promotion_commands,
    render_capability_trust_promotion_audit_line,
    write_capability_trust_promotion_audit,
)
from agent_os.capability_automatic_retry import (
    format_recommended_commands as format_automatic_retry_commands,
    render_capability_automatic_retry_audit_line,
    write_capability_automatic_retry_audit,
)
from agent_os.capability_real_cost_tracking import (
    format_recommended_commands as format_real_cost_tracking_commands,
    render_capability_real_cost_tracking_audit_line,
    write_capability_real_cost_tracking_audit,
)
from agent_os.hosted_dashboard_proof import (
    format_recommended_commands as format_hosted_dashboard_commands,
    render_hosted_dashboard_proof_checklist_line,
    write_hosted_dashboard_proof_checklist,
)
from agent_os.remote_worker_proof import (
    format_recommended_commands as format_remote_worker_commands,
    render_remote_worker_proof_checklist_line,
    write_remote_worker_proof_checklist,
)
from agent_os.autonomous_scheduling_proof import (
    format_recommended_commands as format_autonomous_scheduling_commands,
    render_autonomous_scheduling_proof_checklist_line,
    write_autonomous_scheduling_proof_checklist,
)
from agent_os.browser_desktop_adapter_proof import (
    format_recommended_commands as format_browser_desktop_adapter_commands,
    render_browser_desktop_adapter_proof_checklist_line,
    write_browser_desktop_adapter_proof_checklist,
)
from agent_os.ci_deploy_proof import (
    format_recommended_commands as format_ci_deploy_commands,
    render_ci_deploy_proof_checklist_line,
    write_ci_deploy_proof_checklist,
)
from agent_os.ci_deploy_evidence import record_ci_deploy_evidence
from agent_os.context_pack import ContextPackError, generate_context_pack
from agent_os.coder_prep import (
    CoderPrepError,
    prepare_coder_from_handoff,
    render_coder_prep_cli_lines,
)
from agent_os.coder_worktree_plan import (
    CoderWorktreePlanError,
    prepare_worktree_plan_from_coder_prep,
    render_coder_worktree_plan_cli_lines,
)
from agent_os.budget_enforcement_proof import (
    format_recommended_commands as format_budget_enforcement_commands,
    render_budget_enforcement_proof_checklist_line,
    write_budget_enforcement_proof_checklist,
)
from agent_os.trust_promotion_proof import (
    format_recommended_commands as format_trust_promotion_proof_commands,
    render_trust_promotion_proof_checklist_line,
    write_trust_promotion_proof_checklist,
)
from agent_os.automatic_retry_proof import (
    format_recommended_commands as format_automatic_retry_proof_commands,
    render_automatic_retry_proof_checklist_line,
    write_automatic_retry_proof_checklist,
)
from agent_os.real_cost_tracking_proof import (
    format_recommended_commands as format_real_cost_tracking_proof_commands,
    render_real_cost_tracking_proof_checklist_line,
    write_real_cost_tracking_proof_checklist,
)
from agent_os.goal_completion_audit import (
    format_recommended_commands as format_goal_completion_audit_commands,
    render_goal_completion_audit_line,
    write_goal_completion_audit,
)
from agent_os.expansion_decision_brief import (
    render_expansion_decision_brief_line,
    write_expansion_decision_brief,
)
from agent_os.expansion_decision_evidence_index import (
    render_expansion_decision_evidence_index_line,
    write_expansion_decision_evidence_index,
)
from agent_os.expansion_operator_review_checklist import (
    format_allowed_actions as format_operator_review_allowed_actions,
    render_expansion_operator_review_checklist_line,
    write_expansion_operator_review_checklist,
)
from agent_os.expansion_operator_decision_ledger import (
    format_allowed_actions as format_operator_decision_allowed_actions,
    render_expansion_operator_decision_ledger_line,
    write_expansion_operator_decision_ledger,
)
from agent_os.expansion_operator_approval_draft import (
    format_allowed_actions as format_operator_approval_allowed_actions,
    render_expansion_operator_approval_draft_line,
    write_expansion_operator_approval_draft,
)
from agent_os.expansion_operator_approval_request_review import (
    render_expansion_operator_approval_request_review_line,
    write_expansion_operator_approval_request_review,
)
from agent_os.expansion_operator_approval_schema_decision import (
    render_expansion_operator_approval_schema_decision_line,
    write_expansion_operator_approval_schema_decision,
)
from agent_os.expansion_operator_approval_schema_migration_plan import (
    render_expansion_operator_approval_schema_migration_plan_line,
    write_expansion_operator_approval_schema_migration_plan,
)
from agent_os.expansion_operator_approval_schema_migration_approval_request import (
    format_allowed_actions as format_schema_migration_approval_actions,
    render_expansion_operator_approval_schema_migration_approval_request_line,
    write_expansion_operator_approval_schema_migration_approval_request,
)
from agent_os.expansion_operator_approval_schema_migration_decision_ledger import (
    format_allowed_actions as format_schema_migration_decision_actions,
    render_expansion_operator_approval_schema_migration_decision_ledger_line,
    write_expansion_operator_approval_schema_migration_decision_ledger,
)
from agent_os.expansion_operator_approval_schema_migration_action_checklist import (
    format_allowed_actions as format_schema_migration_action_actions,
    render_expansion_operator_approval_schema_migration_action_checklist_line,
    write_expansion_operator_approval_schema_migration_action_checklist,
)
from agent_os.expansion_operator_approval_schema_migration_selection_packet import (
    format_allowed_actions as format_schema_migration_selection_actions,
    render_expansion_operator_approval_schema_migration_selection_packet_line,
    write_expansion_operator_approval_schema_migration_selection_packet,
)
from agent_os.expansion_operator_approval_schema_migration_selection_input_template import (
    format_allowed_actions as format_schema_migration_input_template_actions,
    render_expansion_operator_approval_schema_migration_selection_input_template_line,
    write_expansion_operator_approval_schema_migration_selection_input_template,
)
from agent_os.operator_approval_schema_migration import (
    apply_operator_approval_schema_migration,
    render_operator_approval_schema_migration_application_line,
)
from agent_os.operator_approval_request_rows import (
    apply_operator_approval_request_rows,
    render_operator_approval_request_rows_application_line,
)
from agent_os.operator_approval_request_decisions import (
    decide_operator_approval_requests,
    render_operator_approval_request_decision_line,
)
from agent_os.operator_approval_effect_proposals import (
    render_operator_approval_effect_proposal_line,
    write_operator_approval_effect_proposals,
)
from agent_os.operator_approval_effect_application import (
    apply_operator_approval_effects,
    render_operator_approval_effect_application_line,
)
from agent_os.capability_activation_tasks import (
    render_capability_activation_task_batch_line,
    write_capability_activation_tasks,
)
from agent_os.capability_activation_contracts import (
    render_capability_activation_contract_batch_line,
    write_capability_activation_contracts,
)
from agent_os.capability_activation_evidence import (
    decide_capability_activation_contracts,
    record_capability_activation_evidence,
    render_capability_activation_decision_line,
    render_capability_activation_evidence_batch_line,
)
from agent_os.capability_activation_followups import (
    render_capability_activation_followup_batch_line,
    write_capability_activation_followups,
)
from agent_os.capability_activation_followup_delegations import (
    render_capability_activation_followup_delegation_batch_line,
    write_capability_activation_followup_delegations,
)
from agent_os.capability_activation_followup_results import (
    render_capability_activation_followup_result_batch_line,
    write_capability_activation_followup_results,
)
from agent_os.capability_activation_followup_result_decisions import (
    decide_capability_activation_followup_results,
    render_capability_activation_followup_result_decision_line,
)
from agent_os.capability_activation_followup_result_effect_proposals import (
    render_capability_activation_followup_result_effect_proposal_line,
    write_capability_activation_followup_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_effect_application import (
    apply_capability_activation_followup_result_effects,
    render_capability_activation_followup_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_tasks import (
    render_capability_activation_followup_result_task_batch_line,
    write_capability_activation_followup_result_tasks,
)
from agent_os.capability_activation_followup_result_task_delegations import (
    render_capability_activation_followup_result_task_delegation_batch_line,
    write_capability_activation_followup_result_task_delegations,
)
from agent_os.capability_activation_followup_result_task_results import (
    render_capability_activation_followup_result_task_result_batch_line,
    write_capability_activation_followup_result_task_results,
)
from agent_os.capability_activation_followup_result_task_result_decisions import (
    decide_capability_activation_followup_result_task_results,
    render_capability_activation_followup_result_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decisions import (
    decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application import (
    apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects,
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations,
)
from agent_os.capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results import (
    render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line,
    write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results,
)
from agent_os.capability_proof_gap import (
    format_recommended_commands as format_proof_gap_commands,
    render_capability_proof_gap_index_line,
    write_capability_proof_gap_index,
)
from agent_os.capability_readiness import (
    format_recommended_commands as format_readiness_commands,
    render_capability_readiness_review_line,
    write_capability_readiness_review,
)
from agent_os.dashboard import generate_static_dashboard
from agent_os.coding_workflow import commit_approved_effect, run_worktree_coding_goal
from agent_os.delegation_runner import (
    DelegationRunError,
    configure_profile_adapter,
    run_delegation,
)
from agent_os.dispatch_posture_history import (
    render_dispatch_posture_history_line,
    write_dispatch_posture_history_report,
)
from agent_os.dispatch_posture_refresh import (
    format_recommended_commands,
    render_dispatch_posture_refresh_line,
    write_dispatch_posture_refresh_report,
)
from agent_os.dispatch_posture_staleness import (
    format_optional_age_seconds,
    render_dispatch_posture_staleness_line,
    write_dispatch_posture_staleness_report,
)
from agent_os.engine import AgentSystem
from agent_os.eval import run_first_milestone_eval
from agent_os.eval_after_change import run_eval_after_change
from agent_os.handoff_review import (
    render_stale_handoff_line,
    write_handoff_review_report,
)
from agent_os.implementation_handoff import (
    render_implementation_handoff_cli_lines,
    summarize_implementation_handoff,
)
from agent_os.github_handoff import create_github_handoff
from agent_os.iteration import generate_next_iteration_packet
from agent_os.learning_distillation import (
    DEFAULT_MIN_OCCURRENCES,
    distill_learnings,
    render_stable_learning_line,
)
from agent_os.memory_entries import (
    MemoryEntryError,
    approve_memory_entry,
    archive_memory_entry,
    propose_memory_entry,
    propose_memory_from_delegation,
    render_memory_entry_line,
)
from agent_os.playbooks import (
    DEFAULT_MIN_SUCCESSES,
    promote_successful_run_playbooks,
    render_playbook_line,
)
from agent_os.planning import (
    PlanningError,
    create_contract_for_goal,
    create_goal_lifecycle,
    ensure_latest_plan,
    render_plan_step_line,
    render_task_line,
    replan_goal,
    update_lifecycle_task,
)
from agent_os.skill_entries import (
    SkillEntryError,
    approve_skill,
    archive_skill,
    propose_skill,
    render_skill_line,
)
from agent_os.profile_routing import (
    RouteRequest,
    ensure_default_profiles,
    format_profile_line,
    route_work,
    write_default_profile_config,
)
from agent_os.project_registry import (
    list_project_registry,
    load_project_context,
    register_project,
    render_project_line,
    render_project_status_lines,
    write_project_context,
)
from agent_os.queue_health import render_queue_health_finding, write_queue_health_report
from agent_os.run_review import (
    evidence_packet_dir,
    write_evidence_index,
    write_replay_summary,
    write_run_review,
)
from agent_os.runtime import detect_runtime_capabilities, write_runtime_capability_matrix
from agent_os.steering import (
    collect_inbox_items,
    next_action_for_target,
    render_steering_review_line,
    write_steering_review,
)
from agent_os.subagent_delegation import (
    DelegationError,
    create_subagent_delegation,
    load_delegation_result_metadata,
    record_delegation_result,
    render_subagent_delegation_line,
)
from agent_os.task_recommendations import (
    render_task_recommendation_line,
    write_task_recommendations,
)
from agent_os.task_runner import TaskRunError, run_planned_task
from agent_os.worktree_cleanup import cleanup_worktrees


PRIMARY_COMMAND_METAVAR = (
    "{init,dashboard,goal,tasks,delegate,context-pack,run-delegation,"
    "implementation-handoff,coder-prep,coder-worktree-plan,review,...}"
)


LEGACY_PROOF_LADDER_COMMANDS = {
    "budget-trust-posture",
    "dispatch-posture-history",
    "dispatch-posture-staleness",
    "dispatch-posture-refresh",
    "goal-completion-audit",
    "hosted-dashboard-proof-checklist",
    "remote-worker-proof-checklist",
    "autonomous-scheduling-proof-checklist",
    "browser-desktop-adapter-proof-checklist",
    "ci-deploy-proof-checklist",
    "budget-enforcement-proof-checklist",
    "trust-promotion-proof-checklist",
    "automatic-retry-proof-checklist",
    "real-cost-tracking-proof-checklist",
}


def _is_legacy_proof_ladder_command(command: str) -> bool:
    return (
        command in LEGACY_PROOF_LADDER_COMMANDS
        or command.startswith("capability-")
        or command.startswith("expansion-")
    )


def _curate_default_help(subparsers) -> None:
    subparsers.metavar = PRIMARY_COMMAND_METAVAR
    subparsers._choices_actions = [
        action
        for action in subparsers._choices_actions
        if not _is_legacy_proof_ladder_command(action.dest)
    ]


def render_eval_candidate_line(candidate) -> str:
    return (
        f"{candidate.id}: {candidate.status} "
        f"source={candidate.source_type}:{candidate.source_id} "
        f"suggested={candidate.suggested_eval} path={candidate.candidate_path}"
    )


def _print_memory_proposal(
    entry,
    artifact_path: Path,
    *,
    already_recorded: bool = False,
) -> None:
    if already_recorded:
        print(f"memory_proposal: already_recorded {entry.id}")
    else:
        print(f"memory_proposal: {entry.id}")
    print(f"project_id: {entry.project_id}")
    print(f"scope: {entry.scope}")
    print(f"key: {entry.key}")
    print(f"value: {entry.value}")
    print(f"source_type: {entry.source_type}")
    print(f"source_id: {entry.source_id}")
    print(f"confidence: {entry.confidence:g}")
    print(f"status: {entry.status}")
    print(f"artifact: {artifact_path}")
    print("network_actions_taken: 0")
    print("external_mutations_taken: 0")


def _print_skill_proposal(
    skill,
    version,
    skill_path: Path,
    *,
    already_recorded: bool = False,
) -> None:
    if already_recorded:
        print(f"skill_proposal: already_recorded {skill.id}")
    else:
        print(f"skill_proposal: {skill.id}")
    print(f"project_id: {skill.project_id or 'none'}")
    print(f"name: {skill.name}")
    print(f"description: {skill.description}")
    print(f"status: {skill.status}")
    print(f"verification_status: {skill.verification_status}")
    print(f"source_run_id: {skill.source_run_id or 'none'}")
    print(f"source_task_id: {skill.source_task_id or 'none'}")
    print(f"path: {skill_path}")
    print(f"version: {version.version}")
    print(f"content_hash: {version.content_hash}")
    print("network_actions_taken: 0")
    print("external_mutations_taken: 0")


def _print_bool(value: object) -> str:
    if value is True:
        return "true"
    if value is False:
        return "false"
    return "unknown"


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        prog="agent-os",
        description=(
            "ClankerOS operator CLI. Primary path: scout a repo, inspect the "
            "implementation handoff, prepare coder work, then review evidence."
        ),
        epilog=(
            "Legacy proof-ladder and report-only expansion commands remain "
            "callable by exact name, but are hidden from default help. See "
            "docs/reference-commands.md for the full command map."
        ),
    )
    parser.add_argument(
        "--root",
        default=".",
        help="Repository root for state and artifacts.",
    )
    subparsers = parser.add_subparsers(dest="command", required=True)

    subparsers.add_parser("init", help="Initialize local harness files and database.")
    subparsers.add_parser("capability-matrix", help="Write the runtime capability matrix.")
    subparsers.add_parser("dashboard", help="Write the static dashboard.")
    subparsers.add_parser("iterate", help="Write the next iteration packet from repo queues.")
    review = subparsers.add_parser("review", help="Write a human-first run review packet.")
    review.add_argument("run_id")
    steer = subparsers.add_parser("steer", help="Write a deterministic steering review.")
    steer.add_argument("goal_id")
    next_action = subparsers.add_parser(
        "next-action",
        help="Recommend the next local operator action for a goal or project.",
    )
    next_action.add_argument("target")
    subparsers.add_parser("inbox", help="List operator-worthy local action items.")
    evidence = subparsers.add_parser("evidence", help="Write a run evidence index.")
    evidence.add_argument("run_id")
    replay_summary = subparsers.add_parser(
        "replay-summary",
        help="Write a conceptual replay summary for a run.",
    )
    replay_summary.add_argument("run_id")
    subparsers.add_parser("approvals", help="List pending approval requests.")
    subparsers.add_parser("handoff-review", help="Review blocked tasks and stale handoffs.")
    subparsers.add_parser("profiles", help="List safe local model/profile routing defaults.")
    profile_show = subparsers.add_parser("profile-show", help="Show a named profile.")
    profile_show.add_argument("name")
    profile_adapter = subparsers.add_parser(
        "profile-adapter",
        help="Configure a local executor adapter for a profile.",
    )
    profile_adapter.add_argument("name")
    profile_adapter.add_argument("--type", default="shell")
    profile_adapter.add_argument("--command", required=True, dest="adapter_command")
    profile_adapter.add_argument(
        "--input-mode",
        choices=["prompt_file", "stdin", "json_file"],
        default="json_file",
    )
    profile_adapter.add_argument(
        "--output-mode",
        choices=["json", "text"],
        default="json",
    )
    profile_adapter.add_argument(
        "--working-directory",
        choices=["system_root", "project_root"],
        default="system_root",
    )
    profile_adapter.add_argument("--timeout-seconds", type=int, default=300)
    route = subparsers.add_parser("route", help="Record a routing decision for a task or category.")
    route.add_argument("task_id", nargs="?")
    route.add_argument("--category")
    route.add_argument("--project")
    route.add_argument("--profile", help="Operator override profile name.")
    delegate = subparsers.add_parser(
        "delegate",
        help="Record a scoped read-only subagent delegation contract for a task.",
    )
    delegate.add_argument("task_id")
    delegate.add_argument("--profile", help="Operator override profile name.")
    delegate.add_argument("--title", required=True)
    delegations = subparsers.add_parser(
        "delegations",
        help="List subagent delegation contracts for a goal.",
    )
    delegations.add_argument("goal_id")
    context_pack = subparsers.add_parser(
        "context-pack",
        help="Generate deterministic repo context for a pending delegation.",
    )
    context_pack.add_argument("delegation_id")
    context_pack.add_argument("--max-files", type=int, default=25)
    context_pack.add_argument("--max-snippets", type=int, default=40)
    context_pack.add_argument("--max-snippet-chars", type=int, default=400)
    context_pack.add_argument("--max-total-chars", type=int, default=20000)
    context_pack.add_argument("--include-glob", action="append", default=[])
    context_pack.add_argument("--exclude-glob", action="append", default=[])
    context_pack.add_argument("--format", choices=["json", "markdown", "both"], default="both")
    delegation_result = subparsers.add_parser(
        "delegation-result",
        help="Show a subagent delegation contract and current result state.",
    )
    delegation_result.add_argument("delegation_id")
    implementation_handoff = subparsers.add_parser(
        "implementation-handoff",
        help="Inspect the implementation handoff artifact for a delegation.",
    )
    implementation_handoff.add_argument("delegation_id")
    coder_prep = subparsers.add_parser(
        "coder-prep",
        help="Write a bounded coder-prep plan from an implementation handoff.",
    )
    coder_prep.add_argument("delegation_id")
    coder_worktree_plan = subparsers.add_parser(
        "coder-worktree-plan",
        help="Write an approval-gated future worktree plan from coder prep.",
    )
    coder_worktree_plan.add_argument("delegation_id")
    run_delegation_parser = subparsers.add_parser(
        "run-delegation",
        help="Execute a pending delegation through a configured local adapter.",
    )
    run_delegation_parser.add_argument("delegation_id")
    run_delegation_parser.add_argument("--profile")
    run_delegation_parser.add_argument("--adapter-command")
    run_delegation_parser.add_argument("--record-memory", action="store_true")
    run_delegation_parser.add_argument("--memory-key")
    run_delegation_parser.add_argument("--operator-id", default="operator")
    record_delegation_result_parser = subparsers.add_parser(
        "record-delegation-result",
        help="Ingest structured read-only subagent output for a delegation.",
    )
    record_delegation_result_parser.add_argument("delegation_id")
    record_delegation_result_parser.add_argument("--summary", required=True)
    record_delegation_result_parser.add_argument("--output-json", required=True)
    record_delegation_result_parser.add_argument(
        "--recorded-by",
        default="operator",
    )
    memory = subparsers.add_parser(
        "memory",
        help="List, propose, approve, and archive durable memory entries.",
    )
    memory_subparsers = memory.add_subparsers(dest="memory_command", required=True)
    memory_list = memory_subparsers.add_parser("list")
    memory_list.add_argument("--project")
    memory_propose = memory_subparsers.add_parser("propose")
    memory_propose.add_argument("--project", required=True)
    memory_propose.add_argument("--key", required=True)
    memory_propose.add_argument("--value", required=True)
    memory_propose.add_argument("--scope", default="project")
    memory_propose.add_argument("--confidence", type=float, default=0.5)
    memory_propose.add_argument("--source-type", default="manual")
    memory_propose.add_argument("--source-id", default="manual")
    memory_propose.add_argument("--created-by-profile", default="operator")
    memory_from_delegation = memory_subparsers.add_parser("propose-from-delegation")
    memory_from_delegation.add_argument("delegation_id")
    memory_from_delegation.add_argument("--key", required=True)
    memory_from_delegation.add_argument("--scope", default="project")
    memory_from_delegation.add_argument("--confidence", type=float, default=0.7)
    memory_from_delegation.add_argument("--created-by-profile")
    memory_approve = memory_subparsers.add_parser("approve")
    memory_approve.add_argument("memory_id")
    memory_approve.add_argument("--approved-by", default="operator")
    memory_archive = memory_subparsers.add_parser("archive")
    memory_archive.add_argument("memory_id")
    memory_archive.add_argument("--archived-by", default="operator")
    memory_archive.add_argument("--reason", default="")
    skills = subparsers.add_parser("skills", help="List proposed and active skills.")
    skills.add_argument("--project")
    skill = subparsers.add_parser(
        "skill",
        help="Propose, approve, archive, and show reusable SKILL.md records.",
    )
    skill_subparsers = skill.add_subparsers(dest="skill_command", required=True)
    skill_propose = skill_subparsers.add_parser("propose")
    skill_propose.add_argument("--project", required=True)
    skill_propose.add_argument("--name", required=True)
    skill_propose.add_argument("--description", required=True)
    skill_propose.add_argument("--from-run", required=True)
    skill_propose.add_argument("--source-task-id")
    skill_propose.add_argument("--created-by-profile", default="operator")
    skill_approve = skill_subparsers.add_parser("approve")
    skill_approve.add_argument("skill_id")
    skill_approve.add_argument("--approved-by", default="operator")
    skill_archive = skill_subparsers.add_parser("archive")
    skill_archive.add_argument("skill_id")
    skill_archive.add_argument("--archived-by", default="operator")
    skill_archive.add_argument("--reason", default="")
    skill_show = skill_subparsers.add_parser("show")
    skill_show.add_argument("skill_id")
    register_project_parser = subparsers.add_parser(
        "register-project",
        help="Register a local git repository for isolated coding-agent work.",
    )
    register_project_parser.add_argument("name")
    register_project_parser.add_argument("--path", required=True)
    register_project_parser.add_argument("--test-command", required=True)
    register_project_parser.add_argument("--allowed-write-root", action="append", default=[])
    subparsers.add_parser(
        "projects",
        help="List registered local git projects.",
    )
    project_status = subparsers.add_parser(
        "project-status",
        help="Show local registry and git status for a registered project.",
    )
    project_status.add_argument("project")
    project_context = subparsers.add_parser(
        "project-context",
        help="Write a durable operator context packet for a registered project.",
    )
    project_context.add_argument("project")
    goal_parser = subparsers.add_parser(
        "goal",
        help="Create a durable project-scoped goal with an initial plan and planned tasks.",
    )
    goal_parser.add_argument("prompt")
    goal_parser.add_argument("--project", required=True)
    plan_parser = subparsers.add_parser(
        "plan",
        help="Show or materialize the latest durable plan for a goal.",
    )
    plan_parser.add_argument("goal_id")
    contract_parser = subparsers.add_parser(
        "contract",
        help="Create or show a sprint contract for the latest goal plan.",
    )
    contract_parser.add_argument("goal_id")
    tasks_parser = subparsers.add_parser(
        "tasks",
        help="List local task records for a goal.",
    )
    tasks_parser.add_argument("goal_id")
    update_task_parser = subparsers.add_parser(
        "update-task",
        help="Update a local task and linked plan-step status.",
    )
    update_task_parser.add_argument("task_id")
    update_task_parser.add_argument("--status", required=True)
    update_task_parser.add_argument("--blocked-reason")
    run_task_parser = subparsers.add_parser(
        "run-task",
        help="Dispatch a planned task through a local profile-backed verifier.",
    )
    run_task_parser.add_argument("task_id")
    run_task_parser.add_argument("--profile")
    task_recommendations_parser = subparsers.add_parser(
        "task-recommendations",
        help="Write retry/replan guidance for failed or blocked planned tasks.",
    )
    task_recommendations_parser.add_argument("--goal")
    replan_parser = subparsers.add_parser(
        "replan",
        help="Create a new plan version for an existing goal.",
    )
    replan_parser.add_argument("goal_id")
    replan_parser.add_argument("--reason", required=True)
    subparsers.add_parser(
        "budget-trust-posture",
        help="Report local budget/trust posture metadata without enforcement.",
    )
    subparsers.add_parser(
        "dispatch-posture-history",
        help="Summarize recent report-only dispatch posture snapshots.",
    )
    dispatch_posture_staleness = subparsers.add_parser(
        "dispatch-posture-staleness",
        help="Review local dispatch posture snapshot freshness.",
    )
    dispatch_posture_staleness.add_argument("--now")
    dispatch_posture_staleness.add_argument("--stale-after-minutes", type=int, default=60)
    subparsers.add_parser(
        "dispatch-posture-refresh",
        help="Recommend manual dispatch posture refresh commands without running them.",
    )
    subparsers.add_parser(
        "capability-expansion-ledger",
        help="Write a report-only ledger for deferred autonomy capabilities.",
    )
    subparsers.add_parser(
        "capability-readiness-review",
        help="Review capability ledger readiness evidence without enabling capabilities.",
    )
    subparsers.add_parser(
        "capability-proof-gap-index",
        help="Index proof gaps from the latest capability readiness review.",
    )
    subparsers.add_parser(
        "capability-approval-boundary-matrix",
        help="Map capability proof gaps to explicit approval boundaries.",
    )
    subparsers.add_parser(
        "capability-evidence-collection-plan",
        help="Plan manual evidence collection from capability approval boundaries.",
    )
    subparsers.add_parser(
        "capability-promotion-gate-checklist",
        help="Review promotion gates from capability evidence collection plans.",
    )
    subparsers.add_parser(
        "capability-promotion-decision-ledger",
        help="Record report-only promotion decisions from promotion gate checklists.",
    )
    subparsers.add_parser(
        "capability-trust-promotion-audit",
        help="Audit trust-promotion readiness from promotion decision ledgers.",
    )
    subparsers.add_parser(
        "capability-automatic-retry-audit",
        help="Audit automatic-retry readiness from trust promotion audits.",
    )
    subparsers.add_parser(
        "capability-real-cost-tracking-audit",
        help="Audit real-cost-tracking readiness from automatic retry audits.",
    )
    subparsers.add_parser(
        "hosted-dashboard-proof-checklist",
        help=(
            "Check hosted-dashboard proof readiness from real cost tracking "
            "proof checklists, falling back to real cost tracking audits."
        ),
    )
    subparsers.add_parser(
        "remote-worker-proof-checklist",
        help="Check remote-worker proof readiness from hosted dashboard proof checklists.",
    )
    subparsers.add_parser(
        "autonomous-scheduling-proof-checklist",
        help="Check autonomous-scheduling proof readiness from remote worker proof checklists.",
    )
    subparsers.add_parser(
        "browser-desktop-adapter-proof-checklist",
        help="Check browser/desktop adapter proof readiness from autonomous scheduling proof checklists.",
    )
    subparsers.add_parser(
        "ci-deploy-proof-checklist",
        help="Check CI Deploy proof readiness from browser/desktop adapter proof checklists.",
    )
    subparsers.add_parser(
        "budget-enforcement-proof-checklist",
        help="Check budget enforcement proof readiness from CI Deploy proof checklists.",
    )
    subparsers.add_parser(
        "trust-promotion-proof-checklist",
        help="Check trust-promotion proof readiness from Budget Enforcement proof checklists.",
    )
    subparsers.add_parser(
        "automatic-retry-proof-checklist",
        help="Check automatic-retry proof readiness from Trust Promotion proof checklists.",
    )
    subparsers.add_parser(
        "real-cost-tracking-proof-checklist",
        help="Check real-cost-tracking proof readiness from Automatic Retry proof checklists.",
    )
    subparsers.add_parser(
        "goal-completion-audit",
        help="Audit the active expansion goal against local proof reports.",
    )
    subparsers.add_parser(
        "expansion-decision-brief",
        help="Summarize operator decisions required by the latest goal completion audit.",
    )
    subparsers.add_parser(
        "expansion-decision-evidence-index",
        help="Index evidence links for decisions in the latest expansion decision brief.",
    )
    subparsers.add_parser(
        "expansion-operator-review-checklist",
        help="Prepare manual operator review choices from the latest expansion evidence index.",
    )
    subparsers.add_parser(
        "expansion-operator-decision-ledger",
        help="Record pending operator decisions from the latest expansion review checklist.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-draft",
        help="Draft approval packets from pending expansion operator decisions without creating approvals.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-request-review",
        help="Review expansion approval drafts against the local approval request contract.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-decision",
        help="Prepare a report-only schema decision from approval request review gaps.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-migration-plan",
        help="Prepare a report-only operator approval schema migration plan.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-migration-approval-request",
        help="Prepare a report-only approval request for the schema migration plan.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-migration-decision-ledger",
        help="Record a report-only pending decision ledger for schema migration approval requests.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-migration-action-checklist",
        help="Prepare a report-only manual action checklist for schema migration decisions.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-migration-selection-packet",
        help="Prepare a report-only operator selection input packet from schema migration action checklists.",
    )
    subparsers.add_parser(
        "expansion-operator-approval-schema-migration-selection-input-template",
        help="Prepare a report-only operator input template from schema migration selection packets.",
    )
    schema_migration_apply = subparsers.add_parser(
        "expansion-operator-approval-schema-migration-apply",
        help="Apply the local operator approval request schema after explicit operator approval.",
    )
    schema_migration_apply.add_argument("--operator-id", required=True)
    schema_migration_apply.add_argument(
        "--selected-action",
        required=True,
        choices=["approve", "defer", "request_more_evidence"],
    )
    schema_migration_apply.add_argument("--selection-note", required=True)
    schema_migration_apply.add_argument("--evidence-reference", required=True)
    request_rows_apply = subparsers.add_parser(
        "expansion-operator-approval-request-rows-apply",
        help="Create local operator approval request rows from approved expansion drafts.",
    )
    request_rows_apply.add_argument("--operator-id", required=True)
    request_rows_apply.add_argument(
        "--selected-action",
        required=True,
        choices=["approve", "defer", "request_more_evidence"],
    )
    request_rows_apply.add_argument("--selection-note", required=True)
    request_rows_apply.add_argument("--evidence-reference", required=True)
    request_decide = subparsers.add_parser(
        "expansion-operator-approval-request-decide",
        help="Record local decisions for pending operator approval request rows.",
    )
    request_decide.add_argument("--operator-id", required=True)
    request_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["approve", "defer", "request_more_evidence"],
    )
    request_decide.add_argument("--selection-note", required=True)
    request_decide.add_argument("--evidence-reference", required=True)
    subparsers.add_parser(
        "expansion-operator-approval-effect-proposals",
        help="Create local proposed effects from approved operator approval request decisions.",
    )
    effect_apply = subparsers.add_parser(
        "expansion-operator-approval-effect-apply",
        help="Apply local operator approval effect proposals as local records only.",
    )
    effect_apply.add_argument("--operator-id", required=True)
    effect_apply.add_argument("--selection-note", required=True)
    effect_apply.add_argument("--evidence-reference", required=True)
    subparsers.add_parser(
        "capability-activation-tasks",
        help="Create pending activation-gate tasks from applied operator approval effects.",
    )
    subparsers.add_parser(
        "capability-activation-contracts",
        help="Record evidence and approval contracts for pending activation-gate tasks.",
    )
    activation_evidence = subparsers.add_parser(
        "capability-activation-evidence",
        help="Attach operator-supplied evidence to capability activation contracts.",
    )
    activation_evidence_target = activation_evidence.add_mutually_exclusive_group(
        required=True,
    )
    activation_evidence_target.add_argument("--all", action="store_true")
    activation_evidence_target.add_argument("--contract-id")
    activation_evidence.add_argument("--evidence-kind", required=True)
    activation_evidence.add_argument("--evidence-reference", required=True)
    activation_evidence.add_argument("--verification-command", required=True)
    activation_evidence.add_argument(
        "--verification-status",
        required=True,
        choices=["pass", "fail", "blocked", "missing", "manual"],
    )
    activation_evidence.add_argument("--recorded-by", default="operator")
    activation_evidence.add_argument("--summary", required=True)
    activation_decide = subparsers.add_parser(
        "capability-activation-decide",
        help="Record operator decisions for evidence-bearing activation contracts.",
    )
    activation_decide.add_argument("--operator-id", required=True)
    activation_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["approve", "defer", "request_more_evidence"],
    )
    activation_decide.add_argument("--selection-note", required=True)
    activation_decide.add_argument("--evidence-reference", required=True)
    subparsers.add_parser(
        "capability-activation-followups",
        help="Create pending follow-up evidence tasks from more-evidence decisions.",
    )
    subparsers.add_parser(
        "capability-activation-followup-delegations",
        help="Create read-only routing and delegation packets for follow-up evidence tasks.",
    )
    subparsers.add_parser(
        "capability-activation-followup-results",
        help="Ingest completed follow-up delegation results as local evidence records.",
    )
    followup_result_decide = subparsers.add_parser(
        "capability-activation-followup-result-decide",
        help="Record operator decisions for ingested follow-up result records.",
    )
    followup_result_decide.add_argument("--operator-id", required=True)
    followup_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_result_decide.add_argument("--selection-note", required=True)
    followup_result_decide.add_argument("--evidence-reference", required=True)
    subparsers.add_parser(
        "capability-activation-followup-result-effect-proposals",
        help=(
            "Create local proposed effects from accepted blocked follow-up result "
            "decisions."
        ),
    )
    followup_result_effect_apply = subparsers.add_parser(
        "capability-activation-followup-result-effect-apply",
        help=(
            "Apply accepted blocked follow-up result effect proposals as local "
            "records only."
        ),
    )
    followup_result_effect_apply.add_argument("--operator-id", required=True)
    followup_result_effect_apply.add_argument("--selection-note", required=True)
    followup_result_effect_apply.add_argument("--evidence-reference", required=True)
    subparsers.add_parser(
        "capability-activation-followup-result-tasks",
        help="Create downstream local tasks from applied follow-up result effects.",
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-delegations",
        help=(
            "Create read-only routing and delegation packets for downstream "
            "follow-up result tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-results",
        help=(
            "Ingest completed downstream follow-up result task delegation "
            "outputs as local evidence records."
        ),
    )
    followup_result_task_result_decide = subparsers.add_parser(
        "capability-activation-followup-result-task-result-decide",
        help=(
            "Record an operator review decision for downstream follow-up "
            "result task evidence records."
        ),
    )
    followup_result_task_result_decide.add_argument("--operator-id", required=True)
    followup_result_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_result_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_result_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-proposals",
        help=(
            "Create local proposed effects from accepted blocked downstream "
            "follow-up result task decisions."
        ),
    )
    followup_task_result_effect_apply = subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-apply",
        help=(
            "Apply downstream follow-up result task decision effect proposals "
            "as local records only."
        ),
    )
    followup_task_result_effect_apply.add_argument("--operator-id", required=True)
    followup_task_result_effect_apply.add_argument("--selection-note", required=True)
    followup_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-tasks",
        help=(
            "Create downstream local tasks from applied downstream follow-up "
            "result decision effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-delegations",
        help=(
            "Create read-only routing and delegation packets for downstream "
            "follow-up result task effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-results",
        help=(
            "Ingest completed downstream follow-up result task effect task "
            "delegation outputs as local evidence records."
        ),
    )
    followup_task_result_effect_task_result_decide = subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-decide",
        help=(
            "Record an operator review decision for downstream follow-up "
            "result task effect task result records."
        ),
    )
    followup_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local proposed effects from accepted blocked downstream "
            "follow-up result task effect task result decisions."
        ),
    )
    followup_task_result_effect_task_result_effect_apply = subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-apply",
        help=(
            "Apply downstream follow-up result task effect task result "
            "decision effect proposals as local records only."
        ),
    )
    followup_task_result_effect_task_result_effect_apply.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_apply.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-tasks",
        help=(
            "Create downstream proof tasks from applied downstream follow-up "
            "result task result effect task result decision effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only delegation packets for downstream result effect "
            "task result effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed downstream result effect task result effect "
            "delegation packets as local result records."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local proposed effects from accepted blocked downstream "
            "result effect task result effect result decisions."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_apply = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-apply",
            help=(
                "Apply downstream result effect task result effect result "
                "decision effect proposals as local records only."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-tasks",
        help=(
            "Create downstream proof tasks from applied downstream result "
            "effect task result effect result decision effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only delegation packets for downstream result effect "
            "task result effect task result effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed downstream result effect task result effect task "
            "result effect delegation packets as local result records."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect task result effect result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local proposed effects from accepted blocked downstream "
            "result effect task result effect task result effect result decisions."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply",
            help=(
                "Apply downstream result effect task result effect task result "
                "effect result decision proposals as local records only."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks",
        help=(
            "Create pending downstream proof tasks from applied downstream "
            "result effect task result effect task result effect task result effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only delegation packets for downstream result effect "
            "task result effect task result effect task result effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed downstream result effect task result effect task "
            "result effect task result effect delegation packets as local "
            "result records."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect task result effect task result "
                "effect result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local proposed effects from accepted blocked downstream "
            "result effect task result effect task result effect task result "
            "effect result decisions."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply",
            help=(
                "Apply downstream result effect task result effect task result "
                "effect task result effect result decision proposals as local "
                "records only."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks",
        help=(
            "Create pending downstream proof tasks from applied downstream "
            "result effect task result effect task result effect task result "
            "effect task result effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only delegation packets for downstream result effect "
            "task result effect task result effect task result effect task "
            "result effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed delegation output for downstream result effect "
            "task result effect task result effect task result effect task "
            "result effect tasks."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect task result effect task result "
                "effect task result effect result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local-only proposed effects from accepted downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result decisions."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply",
            help=(
                "Apply accepted downstream result effect task result effect "
                "task result effect task result effect task result effect "
                "task result effect proposals as local records only."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks",
        help=(
            "Create pending downstream proof tasks from applied downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only evaluator delegation packets for pending "
            "downstream result effect task result effect task result effect "
            "task result effect task result effect task result effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed read-only evaluator outputs for downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task packets."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect task result effect task result "
                "effect task result effect task result effect task result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local-only proposed effects from accepted downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task result decisions."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply",
            help=(
                "Apply accepted downstream result effect task result effect "
                "task result effect task result effect task result effect "
                "task result effect task result effect proposals as local records only."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply.add_argument(
        "--evidence-reference",
        required=True,
    )

    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks",
        help=(
            "Create pending downstream proof tasks from applied downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task result effects."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only delegation packets for downstream result effect "
            "task result effect task result effect task result effect task "
            "result effect task result effect task result effect task result "
            "effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed read-only evaluator outputs for downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task result effect "
            "task result effect task packets."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations",
        help=(
            "Create read-only delegation packets for downstream result effect "
            "task result effect task result effect task result effect task "
            "result effect task result effect task result effect tasks."
        ),
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results",
        help=(
            "Ingest completed read-only evaluator outputs for downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task packets."
        ),
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect task result effect task result "
                "effect task result effect task result effect task result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide",
            help=(
                "Record an operator review decision for downstream result "
                "effect task result effect task result effect task result "
                "effect task result effect task result effect task result effect task result records."
            ),
        )
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--operator-id",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selected-action",
        required=True,
        choices=["accept_keep_blocked", "request_more_evidence", "defer_review"],
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--selection-note",
        required=True,
    )
    followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals",
        help=(
            "Create local proposed effect rows from accepted downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task result decisions."
        ),
    )
    latest_followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application = (
        subparsers.add_parser(
            "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply",
            help=(
                "Apply accepted downstream result effect task result effect "
                "task result effect task result effect task result effect task "
                "result effect task result effect proposals as local records only."
            ),
        )
    )
    latest_followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application.add_argument(
        "--operator-id",
        required=True,
    )
    latest_followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application.add_argument(
        "--selection-note",
        required=True,
    )
    latest_followup_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application.add_argument(
        "--evidence-reference",
        required=True,
    )
    subparsers.add_parser(
        "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks",
        help=(
            "Create pending downstream proof tasks from applied downstream "
            "result effect task result effect task result effect task result "
            "effect task result effect task result effect task result effect "
            "decision effects."
        ),
    )
    approve = subparsers.add_parser("approve", help="Approve a pending local task request.")
    approve.add_argument("approval_id")
    approve.add_argument("--decided-by", default="operator")
    approve.add_argument("--note", default="")

    commit_approved = subparsers.add_parser(
        "commit-approved",
        help="Create the approved local git commit for a verified local_git_commit effect.",
    )
    commit_approved.add_argument("approval_id")
    commit_approved.add_argument("--committed-by", default="operator")
    commit_approved.add_argument("--message")

    cleanup_worktrees_parser = subparsers.add_parser(
        "cleanup-worktrees",
        help="Clean up terminal local coding worktrees after an explicit confirmation.",
    )
    cleanup_worktrees_parser.add_argument("--confirm", action="store_true")
    cleanup_worktrees_parser.add_argument("--decided-by", default="operator")
    cleanup_worktrees_parser.add_argument("--reason", default="operator requested cleanup")

    github_handoff_parser = subparsers.add_parser(
        "github-handoff",
        help="Prepare operator commands for pushing a committed local effect to GitHub.",
    )
    github_handoff_parser.add_argument("effect_id")
    github_handoff_parser.add_argument("--remote", default="origin")
    github_handoff_parser.add_argument("--base", default="main")
    github_handoff_parser.add_argument("--title")

    ci_deploy_evidence_parser = subparsers.add_parser(
        "ci-deploy-evidence",
        help="Record operator-supplied CI/deploy evidence for a GitHub handoff.",
    )
    ci_deploy_evidence_parser.add_argument("github_handoff_id")
    ci_deploy_evidence_parser.add_argument("--provider", required=True)
    ci_deploy_evidence_parser.add_argument("--status", required=True)
    ci_deploy_evidence_parser.add_argument("--external-run-id", required=True)
    ci_deploy_evidence_parser.add_argument("--url", required=True)
    ci_deploy_evidence_parser.add_argument("--recorded-by", default="operator")
    ci_deploy_evidence_parser.add_argument("--note", default="")

    resolve_incident = subparsers.add_parser(
        "resolve-incident",
        help="Resolve an open incident with operator evidence.",
    )
    resolve_incident.add_argument("incident_id")
    resolve_incident.add_argument("--resolved-by", default="operator")
    resolve_incident.add_argument("--note", required=True)

    sweep_stuck = subparsers.add_parser(
        "sweep-stuck",
        help="Detect active tasks older than the timeout and open incidents.",
    )
    sweep_stuck.add_argument("--timeout-seconds", type=int, default=1800)
    sweep_stuck.add_argument("--now", help="Optional ISO timestamp for deterministic sweeps.")

    queue_health = subparsers.add_parser(
        "queue-health",
        help="Report repeated blocked or failed task hotspots.",
    )
    queue_health.add_argument("--blocked-threshold", type=int, default=2)
    queue_health.add_argument("--failed-threshold", type=int, default=2)

    playbooks = subparsers.add_parser(
        "playbooks",
        help="Promote repeated successful eval runs into reusable playbooks.",
    )
    playbooks.add_argument("--min-successes", type=int, default=DEFAULT_MIN_SUCCESSES)

    eval_after_change = subparsers.add_parser(
        "eval-after-change",
        help="Run local evals and record evidence for a harness behavior change.",
    )
    eval_after_change.add_argument("--change", required=True)
    eval_after_change.add_argument("--file", action="append", default=[])

    distill_learnings_parser = subparsers.add_parser(
        "distill-learnings",
        help="Promote stable repeated run learnings into root knowledge.",
    )
    distill_learnings_parser.add_argument(
        "--min-occurrences",
        type=int,
        default=DEFAULT_MIN_OCCURRENCES,
    )

    run_goal = subparsers.add_parser("run-goal", help="Run a goal through the local loop.")
    run_goal.add_argument("description")
    run_goal.add_argument("--project", default="bootstrap")
    run_goal.add_argument("--isolation", choices=["none", "worktree"], default="none")
    run_goal.add_argument("--command", dest="exec_command")

    eval_parser = subparsers.add_parser("eval", help="Run evals.")
    eval_parser.add_argument("--name", default="first_milestone_closed_loop")

    subparsers.add_parser(
        "eval-candidates",
        help="List proposed eval cases discovered from verifier or workflow gaps.",
    )

    subparsers.add_parser("capabilities-json", help="Print detected capabilities as JSON.")
    _curate_default_help(subparsers)
    return parser


def main(argv: list[str] | None = None) -> int:
    parser = build_parser()
    args = parser.parse_args(argv)
    root = Path(args.root).resolve()

    if args.command == "init":
        AgentSystem(root).initialize()
        matrix_path = write_runtime_capability_matrix(root)
        print(f"initialized agent system at {root}")
        print(f"runtime capability matrix: {matrix_path}")
        return 0

    if args.command == "capability-matrix":
        matrix_path = write_runtime_capability_matrix(root)
        print(matrix_path)
        return 0

    if args.command == "dashboard":
        dashboard_path = generate_static_dashboard(root)
        print(dashboard_path)
        return 0

    if args.command == "iterate":
        AgentSystem(root).initialize()
        packet = generate_next_iteration_packet(root)
        print(f"next_iteration: {packet.focus}")
        print(f"section: {packet.source_section}")
        print(f"packet: {packet.packet_path}")
        return 0

    if args.command == "review":
        AgentSystem(root).initialize()
        try:
            report_path, packet = write_run_review(root, args.run_id)
        except KeyError:
            print(f"review_failed: run {args.run_id} not found")
            return 1
        print(f"run_review: {packet.run.id}")
        print(f"run_id: {packet.run.id}")
        print(f"status: {packet.run.status}")
        print(f"review: {report_path.relative_to(root)}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"tasks: {len(packet.tasks)}")
        print(f"events: {len(packet.events)}")
        print(f"recommended_next_action: {packet.recommended_next_action}")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "steer":
        AgentSystem(root).initialize()
        try:
            report_path, review = write_steering_review(root, args.goal_id)
        except KeyError:
            print(f"steer_failed: goal {args.goal_id} not found")
            return 1
        print(f"steering_review: {review.id}")
        print(f"goal_id: {review.goal_id}")
        print(f"run_id: {review.run_id or 'none'}")
        print(f"status: {review.status}")
        print(f"drift_score: {review.drift_score}")
        print(f"recommended_next_action: {review.recommended_next_action}")
        print(f"requires_operator: {'true' if review.requires_operator else 'false'}")
        print(f"report: {report_path.relative_to(root)}")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "next-action":
        AgentSystem(root).initialize()
        try:
            report_path, review = next_action_for_target(root, args.target)
        except KeyError:
            print(f"next_action_failed: target {args.target} not found")
            return 1
        print(f"next_action: {review.recommended_next_action}")
        print("source: steering_review")
        print(f"steering_review: {review.id}")
        print(f"goal_id: {review.goal_id}")
        print(f"run_id: {review.run_id or 'none'}")
        print(f"status: {review.status}")
        print(f"drift_score: {review.drift_score}")
        print(f"requires_operator: {'true' if review.requires_operator else 'false'}")
        print(f"report: {report_path.relative_to(root)}")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "inbox":
        AgentSystem(root).initialize()
        inbox = collect_inbox_items(root)
        steering_reviews = inbox["steering_reviews"]
        pending_approvals = inbox["pending_approvals"]
        open_incidents = inbox["open_incidents"]
        subagent_delegations = inbox["subagent_delegations"]
        print(f"inbox_items: {inbox['count']}")
        print(f"steering_reviews: {len(steering_reviews)}")
        for review in steering_reviews:
            print(render_steering_review_line(review).removeprefix("- "))
        print(f"pending_approvals: {len(pending_approvals)}")
        for approval in pending_approvals:
            print(
                f"approval {approval.id}: goal={approval.goal_id} run={approval.run_id} "
                f"task={approval.task_id} risk={approval.risk_level} reason={approval.reason}"
            )
        print(f"open_incidents: {len(open_incidents)}")
        for incident in open_incidents:
            print(
                f"incident {incident.id}: goal={incident.goal_id or 'none'} "
                f"run={incident.run_id} severity={incident.severity} summary={incident.summary}"
            )
        print(f"subagent_delegations: {len(subagent_delegations)}")
        for delegation in subagent_delegations:
            print(render_subagent_delegation_line(delegation).removeprefix("- "))
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "evidence":
        AgentSystem(root).initialize()
        try:
            report_path, packet = write_evidence_index(root, args.run_id)
        except KeyError:
            print(f"evidence_failed: run {args.run_id} not found")
            return 1
        print(f"evidence_packet: {packet.run.id}")
        print(f"status: {packet.run.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"packet_dir: {evidence_packet_dir(root, packet).relative_to(root)}")
        print(
            "database_rows: "
            f"tasks={len(packet.tasks)} events={len(packet.events)} "
            f"incidents={len(packet.incidents)} approvals={len(packet.approvals)} "
            f"effects={len(packet.effects)}"
        )
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "replay-summary":
        AgentSystem(root).initialize()
        try:
            report_path, packet = write_replay_summary(root, args.run_id)
        except KeyError:
            print(f"replay-summary_failed: run {args.run_id} not found")
            return 1
        print(f"replay_summary: {packet.run.id}")
        print(f"status: {packet.run.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"replay_steps: {len(packet.events)}")
        print("conceptual_replay_only: true")
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "approvals":
        system = AgentSystem(root)
        system.initialize()
        approvals = system.storage.list_pending_approvals()
        print(f"pending_approvals: {len(approvals)}")
        for approval in approvals:
            print(
                f"{approval.id}: task={approval.task_id} run={approval.run_id} "
                f"type={approval.task_type} risk={approval.risk_level} "
                f"reason={approval.reason}"
            )
        return 0

    if args.command == "handoff-review":
        AgentSystem(root).initialize()
        report_path, review = write_handoff_review_report(root)
        print(f"handoff_review: {report_path.relative_to(root)}")
        print(f"status: {review.status}")
        print(f"blocked_tasks: {review.blocked_task_count}")
        print(f"stale_handoffs: {review.stale_handoff_count}")
        for handoff in review.stale_handoffs:
            print(render_stale_handoff_line(handoff).removeprefix("- "))
        return 0

    if args.command == "profiles":
        system = AgentSystem(root)
        system.initialize()
        profiles = ensure_default_profiles(system.storage)
        config_path = write_default_profile_config(root)
        print(f"profiles: {len(profiles)}")
        print(f"config: {config_path.relative_to(root)}")
        for profile in profiles:
            print(format_profile_line(profile))
        return 0

    if args.command == "profile-show":
        system = AgentSystem(root)
        system.initialize()
        ensure_default_profiles(system.storage)
        profile = system.storage.get_profile(args.name)
        if profile is None:
            print(f"profile_missing: {args.name}")
            return 1
        print(f"profile: {profile.name}")
        print(f"label: {profile.label}")
        print(f"model: {profile.model}")
        print(f"cost_tier: {profile.cost_tier}")
        print(f"mode: {profile.mode}")
        print(f"enabled: {profile.enabled}")
        print(f"tools: {','.join(profile.tools_json)}")
        print(f"use_for: {','.join(profile.use_for_json)}")
        for key in sorted(profile.permissions_json):
            print(f"permissions.{key}: {profile.permissions_json[key]}")
        for key in sorted(profile.max_budget_json):
            print(f"max_budget.{key}: {profile.max_budget_json[key]}")
        if profile.adapter_config_json:
            for key in sorted(profile.adapter_config_json):
                print(f"adapter.{key}: {profile.adapter_config_json[key]}")
        else:
            print("adapter: none")
        return 0

    if args.command == "profile-adapter":
        system = AgentSystem(root)
        system.initialize()
        ensure_default_profiles(system.storage)
        try:
            profile = configure_profile_adapter(
                system.storage,
                args.name,
                adapter_type=args.type,
                command=args.adapter_command,
                input_mode=args.input_mode,
                output_mode=args.output_mode,
                timeout_seconds=args.timeout_seconds,
                working_directory=args.working_directory,
            )
        except (DelegationRunError, KeyError) as error:
            print(f"profile_adapter_failed: {error}")
            return 1
        print(f"profile_adapter: {profile.name}")
        print(f"adapter_type: {profile.adapter_config_json.get('type', 'none')}")
        print(f"command: {profile.adapter_config_json.get('command', '')}")
        print(f"input_mode: {profile.adapter_config_json.get('input_mode', '')}")
        print(f"output_mode: {profile.adapter_config_json.get('output_mode', '')}")
        print(
            "working_directory: "
            f"{profile.adapter_config_json.get('working_directory', '')}"
        )
        print(f"timeout_seconds: {profile.adapter_config_json.get('timeout_seconds', '')}")
        return 0

    if args.command == "route":
        if not args.task_id and not args.category:
            print("routing_failed: provide a task_id or --category")
            return 1
        system = AgentSystem(root)
        system.initialize()
        try:
            decision = route_work(
                system.storage,
                RouteRequest(
                    task_id=args.task_id,
                    category=args.category,
                    project_id=args.project,
                    profile_override=args.profile,
                ),
            )
        except KeyError as error:
            print(f"routing_failed: missing {error}")
            return 1
        print(f"routing_decision: {decision.id}")
        print(f"task_id: {decision.task_id or 'none'}")
        print(f"goal_id: {decision.goal_id or 'none'}")
        print(f"project_id: {decision.project_id or 'none'}")
        print(f"category: {decision.category}")
        print(f"selected_profile: {decision.selected_profile}")
        print(f"selected_model: {decision.selected_model}")
        print(f"estimated_cost_tier: {decision.estimated_cost_tier}")
        print(f"status: {decision.status}")
        print(f"reason: {decision.reason}")
        return 0

    if args.command == "delegate":
        system = AgentSystem(root)
        system.initialize()
        try:
            delegation = create_subagent_delegation(
                root,
                system.storage,
                task_id=args.task_id,
                title=args.title,
                profile_override=args.profile,
            )
        except (DelegationError, KeyError) as error:
            print(f"delegation_failed: {error}")
            return 1
        print(f"subagent_delegation: {delegation.id}")
        print(f"routing_decision_id: {delegation.routing_decision_id or 'none'}")
        print(f"parent_goal_id: {delegation.parent_goal_id}")
        print(f"parent_task_id: {delegation.parent_task_id}")
        print(f"assigned_profile: {delegation.assigned_profile}")
        print(f"category: {delegation.category}")
        print(f"title: {delegation.title}")
        print(f"expected_output_schema: {delegation.expected_output_schema}")
        print(f"status: {delegation.status}")
        print(f"execution_started: {str(delegation.started_at is not None).lower()}")
        print(f"artifact: {delegation.result_artifact_path}")
        return 0

    if args.command == "delegations":
        system = AgentSystem(root)
        system.initialize()
        delegations = system.storage.list_subagent_delegations(args.goal_id)
        print(f"delegations: {len(delegations)}")
        for delegation in delegations:
            print(render_subagent_delegation_line(delegation))
        return 0

    if args.command == "context-pack":
        system = AgentSystem(root)
        system.initialize()
        try:
            result = generate_context_pack(
                root,
                system.storage,
                args.delegation_id,
                max_files=args.max_files,
                max_snippets=args.max_snippets,
                max_snippet_chars=args.max_snippet_chars,
                max_total_chars=args.max_total_chars,
                include_globs=args.include_glob,
                exclude_globs=args.exclude_glob,
                output_format=args.format,
            )
        except (ContextPackError, OSError, json.JSONDecodeError) as error:
            print(f"context_pack_failed: {error}")
            return 1
        print(f"context_pack: {result.delegation_id}")
        print(f"context_pack_id: {result.context_pack_id}")
        print(f"delegation_id: {result.delegation_id}")
        print(f"project_id: {result.project_id}")
        print(f"ranked_files: {len(result.payload.get('ranked_files', []))}")
        print(f"grep_hits: {len(result.payload.get('grep_hits', []))}")
        if result.json_path is not None:
            print(f"context_pack_json: {result.json_path.relative_to(root)}")
        if result.markdown_path is not None:
            print(f"context_pack_md: {result.markdown_path.relative_to(root)}")
        print("next_recommended_action: run_delegation")
        return 0

    if args.command == "delegation-result":
        system = AgentSystem(root)
        system.initialize()
        delegation = system.storage.get_subagent_delegation(args.delegation_id)
        if delegation is None:
            print(f"delegation_missing: {args.delegation_id}")
            return 1
        print(f"delegation: {delegation.id}")
        print(f"status: {delegation.status}")
        print(f"assigned_profile: {delegation.assigned_profile}")
        print(f"category: {delegation.category}")
        print(f"parent_goal_id: {delegation.parent_goal_id}")
        print(f"parent_task_id: {delegation.parent_task_id}")
        print(f"expected_output_schema: {delegation.expected_output_schema}")
        print(f"result_summary: {delegation.result_summary or 'none'}")
        print(f"result_artifact_path: {delegation.result_artifact_path}")
        metadata = load_delegation_result_metadata(delegation)
        run_id = metadata.get("execution_run_id") or metadata.get("run_id")
        if run_id:
            print(f"run_id: {run_id}")
        adapter_type = metadata.get("execution_adapter_type") or metadata.get("adapter_type")
        if adapter_type:
            print(f"adapter_type: {adapter_type}")
        evidence_dir = metadata.get("execution_evidence_dir") or metadata.get("evidence_dir")
        if evidence_dir:
            print(f"evidence_packet: {evidence_dir}")
        if metadata.get("context_pack_json"):
            print(f"context_pack: {metadata['context_pack_json']}")
        if metadata.get("context_pack_md"):
            print(f"context_pack_md: {metadata['context_pack_md']}")
        if "context_pack_returned_files_in_inventory" in metadata:
            print(
                "context_pack_returned_files_in_inventory: "
                f"{_print_bool(metadata['context_pack_returned_files_in_inventory'])}"
            )
        if metadata.get("context_pack_returned_files_missing"):
            print(
                "context_pack_returned_files_missing: "
                f"{','.join(metadata['context_pack_returned_files_missing'])}"
            )
        if metadata.get("context_pack_top_ranked_files_referenced"):
            print(
                "context_pack_top_ranked_files_referenced: "
                f"{','.join(metadata['context_pack_top_ranked_files_referenced'])}"
            )
        if metadata.get("implementation_handoff_json"):
            print(f"implementation_handoff: {metadata['implementation_handoff_json']}")
            handoff_summary = summarize_implementation_handoff(root, delegation)
            print(f"implementation_handoff_status: {handoff_summary['status']}")
            print(
                "implementation_handoff_schema_version: "
                f"{handoff_summary['schema_version']}"
            )
            print(f"implementation_handoff_kind: {handoff_summary['kind']}")
            print(
                "implementation_handoff_snippets_embedded: "
                f"{_print_bool(handoff_summary['snippets_embedded'])}"
            )
        if metadata.get("implementation_handoff_md"):
            print(f"implementation_handoff_md: {metadata['implementation_handoff_md']}")
        if "exit_code" in metadata:
            print(f"exit_code: {metadata['exit_code']}")
        if metadata.get("incident_id"):
            print(f"incident_id: {metadata['incident_id']}")
        if "network_actions_taken" in metadata:
            print(f"network_actions_taken: {metadata['network_actions_taken']}")
        if "provider_calls_taken_by_clankeros" in metadata:
            print(
                "provider_calls_taken_by_clankeros: "
                f"{metadata['provider_calls_taken_by_clankeros']}"
            )
        print(f"completed_at: {delegation.completed_at or 'none'}")
        return 0

    if args.command == "implementation-handoff":
        system = AgentSystem(root)
        system.initialize()
        delegation = system.storage.get_subagent_delegation(args.delegation_id)
        if delegation is None:
            print(f"delegation_missing: {args.delegation_id}")
            return 1
        summary = summarize_implementation_handoff(root, delegation)
        for line in render_implementation_handoff_cli_lines(summary):
            print(line)
        if summary["readable"]:
            return 0
        return 1

    if args.command == "coder-prep":
        system = AgentSystem(root)
        system.initialize()
        try:
            result = prepare_coder_from_handoff(root, system.storage, args.delegation_id)
        except CoderPrepError as error:
            print(f"coder_prep_failed: {error}")
            return 1
        for line in render_coder_prep_cli_lines(root, result):
            print(line)
        return 0

    if args.command == "coder-worktree-plan":
        system = AgentSystem(root)
        system.initialize()
        try:
            result = prepare_worktree_plan_from_coder_prep(
                root,
                system.storage,
                args.delegation_id,
            )
        except CoderWorktreePlanError as error:
            print(f"coder_worktree_plan_failed: {error}")
            return 1
        for line in render_coder_worktree_plan_cli_lines(root, result):
            print(line)
        return 0

    if args.command == "run-delegation":
        system = AgentSystem(root)
        system.initialize()
        try:
            result = run_delegation(
                root,
                system.storage,
                args.delegation_id,
                profile_override=args.profile,
                adapter_command=args.adapter_command,
                record_memory=args.record_memory,
                memory_key=args.memory_key,
                operator_id=args.operator_id,
            )
        except (DelegationRunError, KeyError, MemoryEntryError) as error:
            print(f"run_delegation_failed: {error}")
            return 1
        if result.status == "completed":
            print(f"run_delegation: {result.delegation_id}")
        else:
            print(f"run_delegation_failed: {result.message}")
        print(f"delegation_id: {result.delegation_id}")
        print(f"run_id: {result.run_id}")
        print(f"adapter_type: {result.adapter_type}")
        print(f"status: {result.status}")
        print(f"exit_code: {result.exit_code if result.exit_code is not None else 'none'}")
        if result.result_artifact_path is not None:
            print(f"result_artifact: {result.result_artifact_path}")
        print(f"evidence_packet: {result.evidence_dir.relative_to(root)}")
        if result.incident_id:
            print(f"incident_id: {result.incident_id}")
        if result.memory_proposal_id:
            print(f"memory_proposal: {result.memory_proposal_id}")
            print("memory_status: proposed")
        print(f"next_recommended_action: {result.next_recommended_action}")
        if result.status != "completed":
            return 1
        return 0

    if args.command == "record-delegation-result":
        system = AgentSystem(root)
        system.initialize()
        try:
            structured_output = json.loads(args.output_json)
            delegation, already_recorded = record_delegation_result(
                root,
                system.storage,
                delegation_id=args.delegation_id,
                result_summary=args.summary,
                structured_output=structured_output,
                recorded_by=args.recorded_by,
            )
        except json.JSONDecodeError as error:
            print(f"delegation_result_failed: invalid output json: {error}")
            return 1
        except (DelegationError, KeyError, OSError, ValueError) as error:
            print(f"delegation_result_failed: {error}")
            return 1
        if already_recorded:
            print(f"delegation_result_recorded: already_recorded {delegation.id}")
        else:
            print(f"delegation_result_recorded: {delegation.id}")
        print(f"status: {delegation.status}")
        print(f"result_summary: {delegation.result_summary or 'none'}")
        print(f"result_artifact: {delegation.result_artifact_path}")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "memory":
        system = AgentSystem(root)
        system.initialize()
        if args.memory_command == "list":
            entries = system.storage.list_memory_entries(project_id=args.project)
            print(f"memory_entries: {len(entries)}")
            for entry in entries:
                print(render_memory_entry_line(entry))
            return 0
        if args.memory_command == "propose":
            try:
                entry, artifact_path, already_recorded = propose_memory_entry(
                    root,
                    system.storage,
                    project_id=args.project,
                    key=args.key,
                    value=args.value,
                    scope=args.scope,
                    source_type=args.source_type,
                    source_id=args.source_id,
                    confidence=args.confidence,
                    created_by_profile=args.created_by_profile,
                )
            except (MemoryEntryError, OSError) as error:
                print(f"memory_proposal_failed: {error}")
                return 1
            _print_memory_proposal(entry, artifact_path, already_recorded=already_recorded)
            return 0
        if args.memory_command == "propose-from-delegation":
            try:
                entry, artifact_path, already_recorded = propose_memory_from_delegation(
                    root,
                    system.storage,
                    delegation_id=args.delegation_id,
                    key=args.key,
                    scope=args.scope,
                    confidence=args.confidence,
                    created_by_profile=args.created_by_profile,
                )
            except (MemoryEntryError, KeyError, OSError, json.JSONDecodeError) as error:
                print(f"memory_proposal_failed: {error}")
                return 1
            _print_memory_proposal(entry, artifact_path, already_recorded=already_recorded)
            return 0
        if args.memory_command == "approve":
            try:
                entry = approve_memory_entry(
                    system.storage,
                    args.memory_id,
                    approved_by=args.approved_by,
                )
            except MemoryEntryError as error:
                print(f"memory_approve_failed: {error}")
                return 1
            print(f"memory_approved: {entry.id}")
            print(f"status: {entry.status}")
            print(f"approved_by: {args.approved_by}")
            return 0
        if args.memory_command == "archive":
            try:
                entry = archive_memory_entry(
                    system.storage,
                    args.memory_id,
                    archived_by=args.archived_by,
                    reason=args.reason,
                )
            except MemoryEntryError as error:
                print(f"memory_archive_failed: {error}")
                return 1
            print(f"memory_archived: {entry.id}")
            print(f"status: {entry.status}")
            print(f"archived_by: {args.archived_by}")
            print(f"reason: {args.reason or 'none'}")
            return 0

    if args.command == "skills":
        system = AgentSystem(root)
        system.initialize()
        skills = system.storage.list_skills(project_id=args.project)
        print(f"skills: {len(skills)}")
        for skill in skills:
            print(render_skill_line(skill))
        return 0

    if args.command == "skill":
        system = AgentSystem(root)
        system.initialize()
        if args.skill_command == "propose":
            try:
                skill, version, skill_path, already_recorded = propose_skill(
                    root,
                    system.storage,
                    project_id=args.project,
                    name=args.name,
                    description=args.description,
                    source_run_id=args.from_run,
                    source_task_id=args.source_task_id,
                    created_by_profile=args.created_by_profile,
                )
            except (SkillEntryError, OSError) as error:
                print(f"skill_proposal_failed: {error}")
                return 1
            _print_skill_proposal(
                skill,
                version,
                skill_path,
                already_recorded=already_recorded,
            )
            return 0
        if args.skill_command == "approve":
            try:
                skill = approve_skill(
                    system.storage,
                    args.skill_id,
                    approved_by=args.approved_by,
                )
            except SkillEntryError as error:
                print(f"skill_approve_failed: {error}")
                return 1
            print(f"skill_approved: {skill.id}")
            print(f"status: {skill.status}")
            print(f"approved_by: {args.approved_by}")
            return 0
        if args.skill_command == "archive":
            try:
                skill = archive_skill(
                    system.storage,
                    args.skill_id,
                    archived_by=args.archived_by,
                    reason=args.reason,
                )
            except SkillEntryError as error:
                print(f"skill_archive_failed: {error}")
                return 1
            print(f"skill_archived: {skill.id}")
            print(f"status: {skill.status}")
            print(f"archived_by: {args.archived_by}")
            print(f"reason: {args.reason or 'none'}")
            return 0
        if args.skill_command == "show":
            skill = system.storage.get_skill(args.skill_id)
            if skill is None:
                print(f"skill_show_failed: skill {args.skill_id} not found")
                return 1
            print(f"skill: {skill.id}")
            print(f"status: {skill.status}")
            print(f"project_id: {skill.project_id or 'none'}")
            print(f"name: {skill.name}")
            print(f"path: {skill.path}")
            print("")
            try:
                print(Path(skill.path).read_text(encoding="utf-8"), end="")
            except OSError as error:
                print(f"skill_show_failed: {error}")
                return 1
            return 0

    if args.command == "register-project":
        AgentSystem(root).initialize()
        try:
            project = register_project(
                root,
                name=args.name,
                repo_path=Path(args.path),
                default_test_command=args.test_command,
                allowed_write_roots=[
                    Path(path) for path in args.allowed_write_root
                ]
                or None,
            )
        except ValueError as error:
            print(f"project_registration_failed: {error}")
            return 1
        print(f"project_registered: {project.name}")
        print(f"root_path: {project.root_path}")
        print(f"default_test_command: {project.default_test_command}")
        print(f"allowed_write_roots: {','.join(project.allowed_write_roots)}")
        print(f"project_note: projects/{project.name}/project.md")
        return 0

    if args.command == "projects":
        AgentSystem(root).initialize()
        projects = list_project_registry(root)
        print(f"projects: {len(projects)}")
        if not projects:
            print("- none")
        for project in projects:
            print(render_project_line(project))
        return 0

    if args.command == "project-status":
        AgentSystem(root).initialize()
        try:
            context = load_project_context(root, args.project)
        except (KeyError, ValueError) as error:
            detail = error.args[0] if error.args else args.project
            print(f"project_status_failed: project {detail} not found")
            return 1
        for line in render_project_status_lines(context):
            print(line)
        return 0

    if args.command == "project-context":
        AgentSystem(root).initialize()
        try:
            context, context_path = write_project_context(root, args.project)
        except (KeyError, ValueError) as error:
            detail = error.args[0] if error.args else args.project
            print(f"project_context_failed: project {detail} not found")
            return 1
        print(f"project_context: {context.project.name}")
        print(f"context: {context_path.relative_to(root.resolve())}")
        print(f"project_note: {context.project_note}")
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "goal":
        system = AgentSystem(root)
        system.initialize()
        try:
            lifecycle = create_goal_lifecycle(
                root,
                system.storage,
                project_id=args.project,
                prompt=args.prompt,
            )
        except (PlanningError, KeyError) as error:
            print(f"goal_failed: {error}")
            return 1
        print(f"goal_created: {lifecycle.goal.id}")
        print(f"goal_id: {lifecycle.goal.id}")
        print(f"project: {lifecycle.goal.project_id}")
        print(f"title: {lifecycle.goal.title}")
        print(f"plan_id: {lifecycle.plan.id}")
        print(f"plan_version: {lifecycle.plan.version}")
        print(f"tasks: {len(lifecycle.tasks)}")
        print(f"goal_artifact: {lifecycle.goal_artifact_path.relative_to(root)}")
        print(f"plan_artifact: {lifecycle.plan_artifact_path.relative_to(root)}")
        print(f"tasks_artifact: {lifecycle.tasks_artifact_path.relative_to(root)}")
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "plan":
        system = AgentSystem(root)
        system.initialize()
        try:
            goal, plan, steps = ensure_latest_plan(root, system.storage, args.goal_id)
        except (PlanningError, KeyError) as error:
            print(f"plan_failed: {error}")
            return 1
        print(f"plan: {plan.id}")
        print(f"goal_id: {goal.id}")
        print(f"project: {goal.project_id}")
        print(f"version: {plan.version}")
        print(f"status: {plan.status}")
        print(f"summary: {plan.summary}")
        print(f"artifact: {plan.artifact_path}")
        print(f"steps: {len(steps)}")
        for step in steps:
            print(render_plan_step_line(step))
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "contract":
        system = AgentSystem(root)
        system.initialize()
        try:
            goal, plan, contract = create_contract_for_goal(
                root,
                system.storage,
                args.goal_id,
            )
        except (PlanningError, KeyError) as error:
            print(f"contract_failed: {error}")
            return 1
        print(f"contract: {contract.id}")
        print(f"goal_id: {goal.id}")
        print(f"project: {goal.project_id}")
        print(f"plan_id: {plan.id}")
        print(f"plan_version: {plan.version}")
        print(f"status: {contract.status}")
        print(f"artifact: {contract.artifact_path}")
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "tasks":
        system = AgentSystem(root)
        system.initialize()
        try:
            goal = system.storage.get_goal(args.goal_id)
        except KeyError as error:
            print(f"tasks_failed: {error}")
            return 1
        tasks = system.storage.list_tasks(goal.id)
        print(f"tasks: {len(tasks)}")
        print(f"goal_id: {goal.id}")
        print(f"project: {goal.project_id}")
        for task in tasks:
            print(render_task_line(task))
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "update-task":
        system = AgentSystem(root)
        system.initialize()
        try:
            task, step = update_lifecycle_task(
                root,
                system.storage,
                args.task_id,
                status=args.status,
                blocked_reason=args.blocked_reason,
            )
        except (PlanningError, KeyError) as error:
            print(f"task_update_failed: {error}")
            return 1
        print(f"task_updated: {task.id}")
        print(f"goal_id: {task.goal_id}")
        print(f"project: {task.project_id}")
        print(f"status: {task.status}")
        print(f"plan_step_id: {step.id if step else 'none'}")
        print(f"blocked_reason: {args.blocked_reason or 'none'}")
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "run-task":
        system = AgentSystem(root)
        system.initialize()
        try:
            result = run_planned_task(
                root,
                system.storage,
                args.task_id,
                profile_name=args.profile,
            )
        except (TaskRunError, KeyError) as error:
            print(f"run_task_failed: {error}")
            return 1
        print(f"task_run: {result.run_id}")
        print(f"run_id: {result.run_id}")
        print(f"task_id: {result.task.id}")
        print(f"goal_id: {result.goal.id}")
        print(f"project_id: {result.project.name}")
        print(f"profile: {result.profile.name}")
        print(f"status: {result.status}")
        print(f"verification_passed: {'true' if result.verification_passed else 'false'}")
        print(f"routing_decision: {result.routing_decision.id}")
        print(f"evidence_packet: {result.evidence_dir.relative_to(root)}")
        print(f"summary: {result.summary_path.relative_to(root)}")
        print("commands_rerun: 1")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "task-recommendations":
        try:
            report_path, batch = write_task_recommendations(root, goal_id=args.goal)
        except KeyError as error:
            print(f"task_recommendations_failed: {error}")
            return 1
        print(f"task_recommendations: {len(batch.recommendations)}")
        print(f"created: {batch.created_count}")
        print(f"existing: {batch.existing_count}")
        print(f"goal_id: {args.goal or 'all'}")
        print(f"report: {report_path.relative_to(root)}")
        for recommendation in batch.recommendations:
            print(render_task_recommendation_line(recommendation).removeprefix("- "))
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "replan":
        system = AgentSystem(root)
        system.initialize()
        try:
            goal, plan, steps, previous_count = replan_goal(
                root,
                system.storage,
                args.goal_id,
                reason=args.reason,
            )
        except (PlanningError, KeyError) as error:
            print(f"replan_failed: {error}")
            return 1
        print(f"replan: {plan.id}")
        print(f"goal_id: {goal.id}")
        print(f"project: {goal.project_id}")
        print(f"plan_version: {plan.version}")
        print(f"previous_plan_versions: {previous_count}")
        print(f"steps: {len(steps)}")
        print(f"reason: {args.reason}")
        print(f"artifact: {plan.artifact_path}")
        print("commands_rerun: 0")
        print("network_actions_taken: 0")
        print("external_mutations_taken: 0")
        return 0

    if args.command == "budget-trust-posture":
        AgentSystem(root).initialize()
        report_path, posture = write_budget_trust_posture_report(root)
        print(f"budget_trust_posture: {posture.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"tasks: {posture.task_count}")
        print(f"budget_state: {posture.budget_state}")
        print(f"trust_state: {posture.trust_state}")
        print(f"risk_counts: {format_risk_counts(posture.risk_counts)}")
        return 0

    if args.command == "dispatch-posture-history":
        AgentSystem(root).initialize()
        report_path, summary = write_dispatch_posture_history_report(root)
        print(f"dispatch_posture_history: {summary.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"snapshots: {summary.snapshot_count}")
        print(f"latest_tasks: {summary.latest_task_count}")
        print(f"task_delta: {summary.task_count_delta}")
        print(f"latest_risk_counts: {format_risk_counts(summary.latest_risk_counts)}")
        print(f"budget_states: {','.join(summary.budget_states) or 'none'}")
        print(f"trust_states: {','.join(summary.trust_states) or 'none'}")
        print(render_dispatch_posture_history_line(summary).removeprefix("- "))
        return 0

    if args.command == "dispatch-posture-staleness":
        AgentSystem(root).initialize()
        report_path, review = write_dispatch_posture_staleness_report(
            root,
            now=args.now,
            stale_after_minutes=args.stale_after_minutes,
        )
        print(f"dispatch_posture_staleness: {review.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"snapshots: {review.snapshot_count}")
        print(f"stale_snapshots: {review.stale_snapshot_count}")
        print(
            "latest_snapshot_age_seconds: "
            f"{format_optional_age_seconds(review.latest_snapshot_age_seconds)}"
        )
        print(f"stale_after_seconds: {review.stale_after_seconds}")
        print(f"latest_tasks: {review.latest_task_count}")
        print(f"latest_risk_counts: {format_risk_counts(review.latest_risk_counts)}")
        print(render_dispatch_posture_staleness_line(review).removeprefix("- "))
        return 0

    if args.command == "dispatch-posture-refresh":
        AgentSystem(root).initialize()
        report_path, recommendation = write_dispatch_posture_refresh_report(root)
        print(f"dispatch_posture_refresh: {recommendation.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_review: {recommendation.source_review_id or 'none'}")
        print(f"source_status: {recommendation.source_review_status}")
        print(f"snapshots: {recommendation.snapshot_count}")
        print(f"stale_snapshots: {recommendation.stale_snapshot_count}")
        print(
            "latest_snapshot_age_seconds: "
            f"{format_optional_age_seconds(recommendation.latest_snapshot_age_seconds)}"
        )
        print(f"stale_after_seconds: {recommendation.stale_after_seconds}")
        print(
            "recommended_commands: "
            f"{format_recommended_commands(recommendation.recommended_commands)}"
        )
        print(render_dispatch_posture_refresh_line(recommendation).removeprefix("- "))
        return 0

    if args.command == "capability-expansion-ledger":
        AgentSystem(root).initialize()
        report_path, ledger = write_capability_expansion_ledger(root)
        print(f"capability_expansion_ledger: {ledger.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"capabilities: {ledger.capability_count}")
        print(f"ready: {ledger.ready_count}")
        print(f"deferred: {ledger.deferred_count}")
        print(f"approval_boundary: {ledger.approval_boundary}")
        print(render_capability_expansion_ledger_line(ledger).removeprefix("- "))
        return 0

    if args.command == "capability-readiness-review":
        AgentSystem(root).initialize()
        report_path, review = write_capability_readiness_review(root)
        print(f"capability_readiness_review: {review.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_ledger: {review.source_ledger_id or 'none'}")
        print(f"source_status: {review.source_ledger_status}")
        print(f"capabilities: {review.capability_count}")
        print(f"ready: {review.ready_count}")
        print(f"not_ready: {review.not_ready_count}")
        print(f"missing_evidence: {review.missing_evidence_count}")
        print(
            "recommended_commands: "
            f"{format_readiness_commands(review.recommended_commands)}"
        )
        print(render_capability_readiness_review_line(review).removeprefix("- "))
        return 0

    if args.command == "capability-proof-gap-index":
        AgentSystem(root).initialize()
        report_path, index = write_capability_proof_gap_index(root)
        print(f"capability_proof_gap_index: {index.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_review: {index.source_review_id or 'none'}")
        print(f"source_status: {index.source_review_status}")
        print(f"capabilities: {index.capability_count}")
        print(f"gaps: {index.gap_count}")
        print(f"missing_evidence: {index.missing_evidence_count}")
        print(f"blocked_capabilities: {index.blocked_capability_count}")
        print(f"next_proofs: {index.next_proof_count}")
        print(
            "recommended_commands: "
            f"{format_proof_gap_commands(index.recommended_commands)}"
        )
        print(render_capability_proof_gap_index_line(index).removeprefix("- "))
        return 0

    if args.command == "capability-approval-boundary-matrix":
        AgentSystem(root).initialize()
        report_path, matrix = write_capability_approval_boundary_matrix(root)
        print(f"capability_approval_boundary_matrix: {matrix.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_index: {matrix.source_index_id or 'none'}")
        print(f"source_status: {matrix.source_index_status}")
        print(f"capabilities: {matrix.capability_count}")
        print(f"boundaries: {matrix.boundary_count}")
        print(f"gaps: {matrix.gap_count}")
        print(f"blocked_capabilities: {matrix.blocked_capability_count}")
        print(f"approvals_required: {matrix.approval_required_count}")
        print(
            "recommended_commands: "
            f"{format_approval_boundary_commands(matrix.recommended_commands)}"
        )
        print(
            render_capability_approval_boundary_matrix_line(matrix).removeprefix(
                "- "
            )
        )
        return 0

    if args.command == "capability-evidence-collection-plan":
        AgentSystem(root).initialize()
        report_path, plan = write_capability_evidence_collection_plan(root)
        print(f"capability_evidence_collection_plan: {plan.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_matrix: {plan.source_matrix_id or 'none'}")
        print(f"source_status: {plan.source_matrix_status}")
        print(f"capabilities: {plan.capability_count}")
        print(f"evidence_items: {plan.evidence_item_count}")
        print(f"manual_collection: {plan.manual_collection_count}")
        print(f"approvals_required: {plan.approval_required_count}")
        print(f"boundaries: {plan.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_evidence_collection_commands(plan.recommended_commands)}"
        )
        print(
            render_capability_evidence_collection_plan_line(plan).removeprefix(
                "- "
            )
        )
        return 0

    if args.command == "capability-promotion-gate-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_capability_promotion_gate_checklist(root)
        print(f"capability_promotion_gate_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_plan: {checklist.source_plan_id or 'none'}")
        print(f"source_status: {checklist.source_plan_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"gates: {checklist.gate_count}")
        print(f"blocked_promotions: {checklist.blocked_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_promotion_gate_commands(checklist.recommended_commands)}"
        )
        print(
            render_capability_promotion_gate_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "capability-promotion-decision-ledger":
        AgentSystem(root).initialize()
        report_path, ledger = write_capability_promotion_decision_ledger(root)
        print(f"capability_promotion_decision_ledger: {ledger.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {ledger.source_checklist_id or 'none'}")
        print(f"source_status: {ledger.source_checklist_status}")
        print(f"capabilities: {ledger.capability_count}")
        print(f"decisions: {ledger.decision_count}")
        print(f"deferred_promotions: {ledger.deferred_promotion_count}")
        print(
            "operator_decisions_required: "
            f"{ledger.operator_decision_required_count}"
        )
        print(f"blocked_promotions: {ledger.blocked_promotion_count}")
        print(f"missing_evidence: {ledger.missing_evidence_count}")
        print(f"approvals_required: {ledger.approval_required_count}")
        print(f"boundaries: {ledger.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_promotion_decision_commands(ledger.recommended_commands)}"
        )
        print(
            render_capability_promotion_decision_ledger_line(
                ledger
            ).removeprefix("- ")
        )
        return 0

    if args.command == "capability-trust-promotion-audit":
        AgentSystem(root).initialize()
        report_path, audit = write_capability_trust_promotion_audit(root)
        print(f"capability_trust_promotion_audit: {audit.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_ledger: {audit.source_ledger_id or 'none'}")
        print(f"source_status: {audit.source_ledger_status}")
        print(f"capabilities: {audit.capability_count}")
        print(f"audits: {audit.audit_count}")
        print(f"blocked_trust_promotions: {audit.blocked_trust_promotion_count}")
        print(f"operator_reviews_required: {audit.operator_review_required_count}")
        print(f"deferred_promotions: {audit.deferred_promotion_count}")
        print(f"missing_evidence: {audit.missing_evidence_count}")
        print(f"approvals_required: {audit.approval_required_count}")
        print(f"boundaries: {audit.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_trust_promotion_commands(audit.recommended_commands)}"
        )
        print(
            render_capability_trust_promotion_audit_line(
                audit
            ).removeprefix("- ")
        )
        return 0

    if args.command == "capability-automatic-retry-audit":
        AgentSystem(root).initialize()
        report_path, audit = write_capability_automatic_retry_audit(root)
        print(f"capability_automatic_retry_audit: {audit.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_audit: {audit.source_audit_id or 'none'}")
        print(f"source_status: {audit.source_audit_status}")
        print(f"capabilities: {audit.capability_count}")
        print(f"audits: {audit.audit_count}")
        print(f"blocked_retries: {audit.blocked_retry_count}")
        print(f"operator_reviews_required: {audit.operator_review_required_count}")
        print(f"blocked_trust_promotions: {audit.blocked_trust_promotion_count}")
        print(f"deferred_promotions: {audit.deferred_promotion_count}")
        print(f"missing_evidence: {audit.missing_evidence_count}")
        print(f"approvals_required: {audit.approval_required_count}")
        print(f"boundaries: {audit.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_automatic_retry_commands(audit.recommended_commands)}"
        )
        print(
            render_capability_automatic_retry_audit_line(
                audit
            ).removeprefix("- ")
        )
        return 0

    if args.command == "capability-real-cost-tracking-audit":
        AgentSystem(root).initialize()
        report_path, audit = write_capability_real_cost_tracking_audit(root)
        print(f"capability_real_cost_tracking_audit: {audit.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_audit: {audit.source_audit_id or 'none'}")
        print(f"source_status: {audit.source_audit_status}")
        print(f"capabilities: {audit.capability_count}")
        print(f"audits: {audit.audit_count}")
        print(f"blocked_cost_tracking: {audit.blocked_cost_tracking_count}")
        print(f"operator_reviews_required: {audit.operator_review_required_count}")
        print(f"blocked_retries: {audit.blocked_retry_count}")
        print(f"blocked_trust_promotions: {audit.blocked_trust_promotion_count}")
        print(f"deferred_promotions: {audit.deferred_promotion_count}")
        print(f"missing_evidence: {audit.missing_evidence_count}")
        print(f"approvals_required: {audit.approval_required_count}")
        print(f"boundaries: {audit.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_real_cost_tracking_commands(audit.recommended_commands)}"
        )
        print(
            render_capability_real_cost_tracking_audit_line(
                audit
            ).removeprefix("- ")
        )
        return 0

    if args.command == "hosted-dashboard-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_hosted_dashboard_proof_checklist(root)
        print(f"hosted_dashboard_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_kind: {checklist.source_kind}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_audit: {checklist.source_audit_id or 'none'}")
        source_status = (
            checklist.source_checklist_status
            if checklist.source_kind == "real_cost_tracking_proof_checklist"
            else checklist.source_audit_status
        )
        print(f"source_status: {source_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_hosted_dashboard_commands(checklist.recommended_commands)}"
        )
        print(
            render_hosted_dashboard_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "remote-worker-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_remote_worker_proof_checklist(root)
        print(f"remote_worker_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_remote_worker_commands(checklist.recommended_commands)}"
        )
        print(
            render_remote_worker_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "autonomous-scheduling-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_autonomous_scheduling_proof_checklist(root)
        print(f"autonomous_scheduling_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_autonomous_scheduling_commands(checklist.recommended_commands)}"
        )
        print(
            render_autonomous_scheduling_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "browser-desktop-adapter-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_browser_desktop_adapter_proof_checklist(root)
        print(f"browser_desktop_adapter_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(f"blocked_adapter_proofs: {checklist.blocked_adapter_proof_count}")
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_browser_desktop_adapter_commands(checklist.recommended_commands)}"
        )
        print(
            render_browser_desktop_adapter_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "ci-deploy-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_ci_deploy_proof_checklist(root)
        print(f"ci_deploy_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(f"blocked_ci_deploy_proofs: {checklist.blocked_ci_deploy_proof_count}")
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(f"blocked_adapter_proofs: {checklist.blocked_adapter_proof_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_ci_deploy_commands(checklist.recommended_commands)}"
        )
        print(
            render_ci_deploy_proof_checklist_line(checklist).removeprefix(
                "- "
            )
        )
        return 0

    if args.command == "budget-enforcement-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_budget_enforcement_proof_checklist(root)
        print(f"budget_enforcement_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(
            "blocked_budget_enforcement_proofs: "
            f"{checklist.blocked_budget_enforcement_proof_count}"
        )
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(f"blocked_ci_deploy_proofs: {checklist.blocked_ci_deploy_proof_count}")
        print(f"blocked_adapter_proofs: {checklist.blocked_adapter_proof_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_budget_enforcement_commands(checklist.recommended_commands)}"
        )
        print(
            render_budget_enforcement_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "trust-promotion-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_trust_promotion_proof_checklist(root)
        print(f"trust_promotion_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(
            "blocked_trust_promotion_proofs: "
            f"{checklist.blocked_trust_promotion_proof_count}"
        )
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(
            "blocked_budget_enforcement_proofs: "
            f"{checklist.blocked_budget_enforcement_proof_count}"
        )
        print(f"blocked_ci_deploy_proofs: {checklist.blocked_ci_deploy_proof_count}")
        print(f"blocked_adapter_proofs: {checklist.blocked_adapter_proof_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_trust_promotion_proof_commands(checklist.recommended_commands)}"
        )
        print(
            render_trust_promotion_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "automatic-retry-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_automatic_retry_proof_checklist(root)
        print(f"automatic_retry_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(
            "blocked_automatic_retry_proofs: "
            f"{checklist.blocked_automatic_retry_proof_count}"
        )
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(
            "blocked_trust_promotion_proofs: "
            f"{checklist.blocked_trust_promotion_proof_count}"
        )
        print(
            "blocked_budget_enforcement_proofs: "
            f"{checklist.blocked_budget_enforcement_proof_count}"
        )
        print(f"blocked_ci_deploy_proofs: {checklist.blocked_ci_deploy_proof_count}")
        print(f"blocked_adapter_proofs: {checklist.blocked_adapter_proof_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_automatic_retry_proof_commands(checklist.recommended_commands)}"
        )
        print(
            render_automatic_retry_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "real-cost-tracking-proof-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_real_cost_tracking_proof_checklist(root)
        print(f"real_cost_tracking_proof_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {checklist.source_checklist_id or 'none'}")
        print(f"source_status: {checklist.source_checklist_status}")
        print(f"capabilities: {checklist.capability_count}")
        print(f"checklist_items: {checklist.checklist_count}")
        print(
            "blocked_real_cost_tracking_proofs: "
            f"{checklist.blocked_real_cost_tracking_proof_count}"
        )
        print(f"operator_reviews_required: {checklist.operator_review_required_count}")
        print(
            "blocked_automatic_retry_proofs: "
            f"{checklist.blocked_automatic_retry_proof_count}"
        )
        print(
            "blocked_trust_promotion_proofs: "
            f"{checklist.blocked_trust_promotion_proof_count}"
        )
        print(
            "blocked_budget_enforcement_proofs: "
            f"{checklist.blocked_budget_enforcement_proof_count}"
        )
        print(f"blocked_ci_deploy_proofs: {checklist.blocked_ci_deploy_proof_count}")
        print(f"blocked_adapter_proofs: {checklist.blocked_adapter_proof_count}")
        print(f"blocked_scheduling_proofs: {checklist.blocked_scheduling_proof_count}")
        print(f"blocked_worker_proofs: {checklist.blocked_worker_proof_count}")
        print(f"blocked_dashboard_proofs: {checklist.blocked_dashboard_proof_count}")
        print(f"blocked_cost_tracking: {checklist.blocked_cost_tracking_count}")
        print(f"blocked_retries: {checklist.blocked_retry_count}")
        print(f"blocked_trust_promotions: {checklist.blocked_trust_promotion_count}")
        print(f"missing_evidence: {checklist.missing_evidence_count}")
        print(f"approvals_required: {checklist.approval_required_count}")
        print(f"boundaries: {checklist.boundary_count}")
        print(
            "recommended_commands: "
            f"{format_real_cost_tracking_proof_commands(checklist.recommended_commands)}"
        )
        print(
            render_real_cost_tracking_proof_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "goal-completion-audit":
        AgentSystem(root).initialize()
        report_path, audit = write_goal_completion_audit(root)
        print(f"goal_completion_audit: {audit.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"requirements: {audit.requirement_count}")
        print(f"satisfied_requirements: {audit.satisfied_requirement_count}")
        print(f"blocked_requirements: {audit.blocked_requirement_count}")
        print(f"missing_evidence: {audit.missing_evidence_count}")
        print(f"approvals_required: {audit.approval_required_count}")
        print(f"external_decisions_required: {audit.external_decision_count}")
        print(
            "recommended_commands: "
            f"{format_goal_completion_audit_commands(audit.recommended_commands)}"
        )
        print(render_goal_completion_audit_line(audit).removeprefix("- "))
        return 0

    if args.command == "expansion-decision-brief":
        AgentSystem(root).initialize()
        report_path, brief = write_expansion_decision_brief(root)
        print(f"expansion_decision_brief: {brief.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_audit: {brief.source_audit_id}")
        print(f"source_status: {brief.source_audit_status}")
        print(f"requirements: {brief.requirement_count}")
        print(f"blocked_requirements: {brief.blocked_requirement_count}")
        print(f"external_decisions_required: {brief.external_decision_count}")
        print(f"approvals_required: {brief.approval_required_count}")
        print(f"decision_items: {brief.decision_item_count}")
        print(f"recommended_next_step: {brief.recommended_next_step}")
        print(render_expansion_decision_brief_line(brief).removeprefix("- "))
        return 0

    if args.command == "expansion-decision-evidence-index":
        AgentSystem(root).initialize()
        report_path, index = write_expansion_decision_evidence_index(root)
        print(f"expansion_decision_evidence_index: {index.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_brief: {index.source_brief_id}")
        print(f"source_status: {index.source_brief_status}")
        print(f"source_audit: {index.source_audit_id}")
        print(f"decision_items: {index.decision_item_count}")
        print(f"evidence_items: {index.evidence_item_count}")
        print(f"external_decisions: {index.external_decision_count}")
        print(f"capability_decisions: {index.capability_decision_count}")
        print(f"missing_evidence_links: {index.missing_evidence_link_count}")
        print(f"recommended_next_step: {index.recommended_next_step}")
        print(render_expansion_decision_evidence_index_line(index).removeprefix("- "))
        return 0

    if args.command == "expansion-operator-review-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = write_expansion_operator_review_checklist(root)
        print(f"expansion_operator_review_checklist: {checklist.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_index: {checklist.source_index_id}")
        print(f"source_status: {checklist.source_index_status}")
        print(f"source_brief: {checklist.source_brief_id}")
        print(f"source_audit: {checklist.source_audit_id}")
        print(f"review_items: {checklist.review_item_count}")
        print(f"decision_required: {checklist.decision_required_count}")
        print(f"external_reviews: {checklist.external_review_count}")
        print(f"capability_reviews: {checklist.capability_review_count}")
        print(f"missing_evidence_links: {checklist.missing_evidence_link_count}")
        print(
            "allowed_actions: "
            f"{format_operator_review_allowed_actions(checklist.allowed_actions)}"
        )
        print(f"recommended_next_step: {checklist.recommended_next_step}")
        print(render_expansion_operator_review_checklist_line(checklist).removeprefix("- "))
        return 0

    if args.command == "expansion-operator-decision-ledger":
        AgentSystem(root).initialize()
        report_path, ledger = write_expansion_operator_decision_ledger(root)
        print(f"expansion_operator_decision_ledger: {ledger.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {ledger.source_checklist_id}")
        print(f"source_status: {ledger.source_checklist_status}")
        print(f"source_index: {ledger.source_index_id}")
        print(f"source_brief: {ledger.source_brief_id}")
        print(f"source_audit: {ledger.source_audit_id}")
        print(f"decision_items: {ledger.decision_item_count}")
        print(f"pending_decisions: {ledger.pending_decision_count}")
        print(f"approved_decisions: {ledger.approved_decision_count}")
        print(f"deferred_decisions: {ledger.deferred_decision_count}")
        print(f"more_evidence_requested: {ledger.more_evidence_requested_count}")
        print(f"external_decisions: {ledger.external_decision_count}")
        print(f"capability_decisions: {ledger.capability_decision_count}")
        print(
            "allowed_actions: "
            f"{format_operator_decision_allowed_actions(ledger.allowed_actions)}"
        )
        print(f"recommended_next_step: {ledger.recommended_next_step}")
        print(render_expansion_operator_decision_ledger_line(ledger).removeprefix("- "))
        return 0

    if args.command == "expansion-operator-approval-draft":
        AgentSystem(root).initialize()
        report_path, draft = write_expansion_operator_approval_draft(root)
        print(f"expansion_operator_approval_draft: {draft.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_ledger: {draft.source_ledger_id}")
        print(f"source_status: {draft.source_ledger_status}")
        print(f"source_checklist: {draft.source_checklist_id}")
        print(f"source_index: {draft.source_index_id}")
        print(f"source_brief: {draft.source_brief_id}")
        print(f"source_audit: {draft.source_audit_id}")
        print(f"draft_items: {draft.draft_item_count}")
        print(f"draft_requests: {draft.draft_request_count}")
        print(f"created_approval_requests: {draft.created_approval_request_count}")
        print(f"external_drafts: {draft.external_draft_count}")
        print(f"capability_drafts: {draft.capability_draft_count}")
        print(f"approval_boundaries: {draft.approval_boundary_count}")
        print(f"pending_decisions: {draft.pending_decision_count}")
        print(
            "allowed_actions: "
            f"{format_operator_approval_allowed_actions(draft.allowed_actions)}"
        )
        print(f"recommended_next_step: {draft.recommended_next_step}")
        print(render_expansion_operator_approval_draft_line(draft).removeprefix("- "))
        return 0

    if args.command == "expansion-operator-approval-request-review":
        AgentSystem(root).initialize()
        report_path, review = write_expansion_operator_approval_request_review(root)
        print(f"expansion_operator_approval_request_review: {review.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_draft: {review.source_draft_id}")
        print(f"source_status: {review.source_draft_status}")
        print(f"source_ledger: {review.source_ledger_id}")
        print(f"source_checklist: {review.source_checklist_id}")
        print(f"source_index: {review.source_index_id}")
        print(f"source_brief: {review.source_brief_id}")
        print(f"source_audit: {review.source_audit_id}")
        print(f"draft_requests: {review.draft_request_count}")
        print(f"review_items: {review.review_item_count}")
        print(f"ready_requests: {review.ready_request_count}")
        print(f"blocked_requests: {review.blocked_request_count}")
        print(f"schema_gaps: {review.schema_gap_count}")
        print(f"created_approval_requests: {review.created_approval_request_count}")
        print(f"existing_approval_requests: {review.existing_approval_request_count}")
        print(f"external_requests: {review.external_request_count}")
        print(f"capability_requests: {review.capability_request_count}")
        print(f"approval_boundaries: {review.approval_boundary_count}")
        print(f"recommended_next_step: {review.recommended_next_step}")
        print(
            render_expansion_operator_approval_request_review_line(
                review
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-decision":
        AgentSystem(root).initialize()
        report_path, decision = write_expansion_operator_approval_schema_decision(root)
        print(f"expansion_operator_approval_schema_decision: {decision.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_review: {decision.source_review_id}")
        print(f"source_status: {decision.source_review_status}")
        print(f"source_draft: {decision.source_draft_id}")
        print(f"source_ledger: {decision.source_ledger_id}")
        print(f"source_checklist: {decision.source_checklist_id}")
        print(f"source_index: {decision.source_index_id}")
        print(f"source_brief: {decision.source_brief_id}")
        print(f"source_audit: {decision.source_audit_id}")
        print(f"affected_requests: {decision.affected_request_count}")
        print(f"schema_gaps: {decision.schema_gap_count}")
        print(f"missing_fields: {decision.missing_field_count}")
        print(f"external_requests: {decision.external_request_count}")
        print(f"capability_requests: {decision.capability_request_count}")
        print(f"decision_options: {decision.decision_option_count}")
        print(f"recommended_option: {decision.recommended_option}")
        print(f"rejected_options: {decision.rejected_option_count}")
        print(f"schema_objects: {decision.schema_object_count}")
        print(f"migration_applied: {decision.migration_applied_count}")
        print(f"created_approval_requests: {decision.created_approval_request_count}")
        print(f"existing_approval_requests: {decision.existing_approval_request_count}")
        print(f"recommended_next_step: {decision.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_decision_line(
                decision
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-migration-plan":
        AgentSystem(root).initialize()
        report_path, plan = write_expansion_operator_approval_schema_migration_plan(
            root
        )
        print(f"expansion_operator_approval_schema_migration_plan: {plan.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_decision: {plan.source_decision_id}")
        print(f"source_status: {plan.source_decision_status}")
        print(f"source_review: {plan.source_review_id}")
        print(f"source_review_status: {plan.source_review_status}")
        print(f"source_draft: {plan.source_draft_id}")
        print(f"source_ledger: {plan.source_ledger_id}")
        print(f"source_checklist: {plan.source_checklist_id}")
        print(f"source_index: {plan.source_index_id}")
        print(f"source_brief: {plan.source_brief_id}")
        print(f"source_audit: {plan.source_audit_id}")
        print(f"recommended_option: {plan.recommended_option}")
        print(f"target_table: {plan.target_table}")
        print(f"affected_requests: {plan.affected_request_count}")
        print(f"schema_gaps: {plan.schema_gap_count}")
        print(f"missing_fields: {plan.missing_field_count}")
        print(f"external_requests: {plan.external_request_count}")
        print(f"capability_requests: {plan.capability_request_count}")
        print(f"planned_columns: {plan.planned_column_count}")
        print(f"planned_indexes: {plan.planned_index_count}")
        print(f"migration_steps: {plan.migration_step_count}")
        print(f"migration_applied: {plan.migration_applied_count}")
        print(f"table_created: {plan.table_created_count}")
        print(f"operator_approval_rows_created: {plan.operator_approval_row_count}")
        print(f"approval_requests_created: {plan.created_approval_request_count}")
        print(f"existing_approval_requests: {plan.existing_approval_request_count}")
        print(f"recommended_next_step: {plan.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_migration_plan_line(
                plan
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-migration-approval-request":
        AgentSystem(root).initialize()
        report_path, request = (
            write_expansion_operator_approval_schema_migration_approval_request(
                root
            )
        )
        print(
            "expansion_operator_approval_schema_migration_approval_request: "
            f"{request.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_plan: {request.source_plan_id}")
        print(f"source_status: {request.source_plan_status}")
        print(f"source_decision: {request.source_decision_id}")
        print(f"source_decision_status: {request.source_decision_status}")
        print(f"source_review: {request.source_review_id}")
        print(f"source_review_status: {request.source_review_status}")
        print(f"target_table: {request.target_table}")
        print(f"planned_columns: {request.planned_column_count}")
        print(f"planned_indexes: {request.planned_index_count}")
        print(f"migration_steps: {request.migration_step_count}")
        print(f"affected_requests: {request.affected_request_count}")
        print(f"schema_gaps: {request.schema_gap_count}")
        print(f"request_count: {request.request_count}")
        print(f"approval_boundary: {request.approval_boundary}")
        print(f"requested_action: {request.requested_action}")
        print(
            "allowed_actions: "
            f"{format_schema_migration_approval_actions(request.allowed_actions)}"
        )
        print(f"migration_applied: {request.migration_applied_count}")
        print(f"table_created: {request.table_created_count}")
        print(f"operator_approval_rows_created: {request.operator_approval_row_count}")
        print(f"approval_requests_created: {request.created_approval_request_count}")
        print(f"existing_approval_requests: {request.existing_approval_request_count}")
        print(f"recommended_next_step: {request.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_migration_approval_request_line(
                request
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-migration-decision-ledger":
        AgentSystem(root).initialize()
        report_path, ledger = (
            write_expansion_operator_approval_schema_migration_decision_ledger(root)
        )
        print(
            "expansion_operator_approval_schema_migration_decision_ledger: "
            f"{ledger.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_request: {ledger.source_request_id}")
        print(f"source_status: {ledger.source_request_status}")
        print(f"source_plan: {ledger.source_plan_id}")
        print(f"source_plan_status: {ledger.source_plan_status}")
        print(f"source_decision: {ledger.source_decision_id}")
        print(f"source_decision_status: {ledger.source_decision_status}")
        print(f"source_review: {ledger.source_review_id}")
        print(f"source_review_status: {ledger.source_review_status}")
        print(f"target_table: {ledger.target_table}")
        print(f"planned_columns: {ledger.planned_column_count}")
        print(f"planned_indexes: {ledger.planned_index_count}")
        print(f"migration_steps: {ledger.migration_step_count}")
        print(f"affected_requests: {ledger.affected_request_count}")
        print(f"schema_gaps: {ledger.schema_gap_count}")
        print(f"request_count: {ledger.request_count}")
        print(f"decision_count: {ledger.decision_count}")
        print(f"pending_decisions: {ledger.pending_decision_count}")
        print(f"approved_decisions: {ledger.approved_decision_count}")
        print(f"deferred_decisions: {ledger.deferred_decision_count}")
        print(f"more_evidence_decisions: {ledger.more_evidence_decision_count}")
        print(f"approval_boundary: {ledger.approval_boundary}")
        print(f"requested_action: {ledger.requested_action}")
        print(
            "allowed_actions: "
            f"{format_schema_migration_decision_actions(ledger.allowed_actions)}"
        )
        print(f"migration_applied: {ledger.migration_applied_count}")
        print(f"table_created: {ledger.table_created_count}")
        print(f"operator_approval_rows_created: {ledger.operator_approval_row_count}")
        print(f"approval_requests_created: {ledger.created_approval_request_count}")
        print(f"existing_approval_requests: {ledger.existing_approval_request_count}")
        print(f"recommended_next_step: {ledger.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_migration_decision_ledger_line(
                ledger
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-migration-action-checklist":
        AgentSystem(root).initialize()
        report_path, checklist = (
            write_expansion_operator_approval_schema_migration_action_checklist(
                root
            )
        )
        print(
            "expansion_operator_approval_schema_migration_action_checklist: "
            f"{checklist.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_ledger: {checklist.source_ledger_id}")
        print(f"source_status: {checklist.source_ledger_status}")
        print(f"source_request: {checklist.source_request_id}")
        print(f"source_request_status: {checklist.source_request_status}")
        print(f"source_plan: {checklist.source_plan_id}")
        print(f"source_plan_status: {checklist.source_plan_status}")
        print(f"source_decision: {checklist.source_decision_id}")
        print(f"source_decision_status: {checklist.source_decision_status}")
        print(f"source_review: {checklist.source_review_id}")
        print(f"source_review_status: {checklist.source_review_status}")
        print(f"target_table: {checklist.target_table}")
        print(f"request_count: {checklist.request_count}")
        print(f"decision_count: {checklist.decision_count}")
        print(f"pending_decisions: {checklist.pending_decision_count}")
        print(f"action_count: {checklist.action_count}")
        print(f"pending_actions: {checklist.pending_action_count}")
        print(f"actions_taken: {checklist.actions_taken_count}")
        print(f"selected_action: {checklist.selected_action}")
        print(f"approval_boundary: {checklist.approval_boundary}")
        print(f"requested_action: {checklist.requested_action}")
        print(
            "allowed_actions: "
            f"{format_schema_migration_action_actions(checklist.allowed_actions)}"
        )
        print(f"migration_applied: {checklist.migration_applied_count}")
        print(f"table_created: {checklist.table_created_count}")
        print(
            "operator_approval_rows_created: "
            f"{checklist.operator_approval_row_count}"
        )
        print(f"approval_requests_created: {checklist.created_approval_request_count}")
        print(f"existing_approval_requests: {checklist.existing_approval_request_count}")
        print(f"recommended_next_step: {checklist.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_migration_action_checklist_line(
                checklist
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-migration-selection-packet":
        AgentSystem(root).initialize()
        report_path, packet = (
            write_expansion_operator_approval_schema_migration_selection_packet(
                root
            )
        )
        print(
            "expansion_operator_approval_schema_migration_selection_packet: "
            f"{packet.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_checklist: {packet.source_checklist_id}")
        print(f"source_status: {packet.source_checklist_status}")
        print(f"source_ledger: {packet.source_ledger_id}")
        print(f"source_ledger_status: {packet.source_ledger_status}")
        print(f"source_request: {packet.source_request_id}")
        print(f"source_request_status: {packet.source_request_status}")
        print(f"source_plan: {packet.source_plan_id}")
        print(f"source_plan_status: {packet.source_plan_status}")
        print(f"source_decision: {packet.source_decision_id}")
        print(f"source_decision_status: {packet.source_decision_status}")
        print(f"source_review: {packet.source_review_id}")
        print(f"source_review_status: {packet.source_review_status}")
        print(f"target_table: {packet.target_table}")
        print(f"request_count: {packet.request_count}")
        print(f"decision_count: {packet.decision_count}")
        print(f"pending_decisions: {packet.pending_decision_count}")
        print(f"action_count: {packet.action_count}")
        print(f"pending_actions: {packet.pending_action_count}")
        print(f"actions_taken: {packet.actions_taken_count}")
        print(f"selected_action: {packet.selected_action}")
        print(f"selection_count: {packet.selection_count}")
        print(f"pending_selections: {packet.pending_selection_count}")
        print(f"selections_recorded: {packet.selections_recorded_count}")
        print(f"approve_selections: {packet.approve_selection_count}")
        print(f"defer_selections: {packet.defer_selection_count}")
        print(f"more_evidence_selections: {packet.more_evidence_selection_count}")
        print(f"approval_boundary: {packet.approval_boundary}")
        print(f"requested_action: {packet.requested_action}")
        print(
            "allowed_actions: "
            f"{format_schema_migration_selection_actions(packet.allowed_actions)}"
        )
        print(f"migration_applied: {packet.migration_applied_count}")
        print(f"table_created: {packet.table_created_count}")
        print(
            "operator_approval_rows_created: "
            f"{packet.operator_approval_row_count}"
        )
        print(f"approval_requests_created: {packet.created_approval_request_count}")
        print(f"existing_approval_requests: {packet.existing_approval_request_count}")
        print(f"recommended_next_step: {packet.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_migration_selection_packet_line(
                packet
            ).removeprefix("- ")
        )
        return 0

    if (
        args.command
        == "expansion-operator-approval-schema-migration-selection-input-template"
    ):
        AgentSystem(root).initialize()
        report_path, template = (
            write_expansion_operator_approval_schema_migration_selection_input_template(
                root
            )
        )
        print(
            "expansion_operator_approval_schema_migration_selection_input_template: "
            f"{template.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_packet: {template.source_packet_id}")
        print(f"source_status: {template.source_packet_status}")
        print(f"source_checklist: {template.source_checklist_id}")
        print(f"source_checklist_status: {template.source_checklist_status}")
        print(f"source_ledger: {template.source_ledger_id}")
        print(f"source_ledger_status: {template.source_ledger_status}")
        print(f"source_request: {template.source_request_id}")
        print(f"source_request_status: {template.source_request_status}")
        print(f"source_plan: {template.source_plan_id}")
        print(f"source_plan_status: {template.source_plan_status}")
        print(f"source_decision: {template.source_decision_id}")
        print(f"source_decision_status: {template.source_decision_status}")
        print(f"source_review: {template.source_review_id}")
        print(f"source_review_status: {template.source_review_status}")
        print(f"target_table: {template.target_table}")
        print(f"request_count: {template.request_count}")
        print(f"decision_count: {template.decision_count}")
        print(f"pending_decisions: {template.pending_decision_count}")
        print(f"action_count: {template.action_count}")
        print(f"pending_actions: {template.pending_action_count}")
        print(f"actions_taken: {template.actions_taken_count}")
        print(f"selected_action: {template.selected_action}")
        print(f"selection_count: {template.selection_count}")
        print(f"pending_selections: {template.pending_selection_count}")
        print(f"selections_recorded: {template.selections_recorded_count}")
        print(f"approve_selections: {template.approve_selection_count}")
        print(f"defer_selections: {template.defer_selection_count}")
        print(f"more_evidence_selections: {template.more_evidence_selection_count}")
        print(f"template_count: {template.template_count}")
        print(f"pending_inputs: {template.pending_input_count}")
        print(f"inputs_recorded: {template.inputs_recorded_count}")
        print(f"required_fields_count: {template.required_fields_count}")
        print(f"missing_required_inputs: {template.missing_required_input_count}")
        print(f"approval_boundary: {template.approval_boundary}")
        print(f"requested_action: {template.requested_action}")
        print(
            "allowed_actions: "
            f"{format_schema_migration_input_template_actions(template.allowed_actions)}"
        )
        print(f"migration_applied: {template.migration_applied_count}")
        print(f"table_created: {template.table_created_count}")
        print(
            "operator_approval_rows_created: "
            f"{template.operator_approval_row_count}"
        )
        print(f"approval_requests_created: {template.created_approval_request_count}")
        print(f"existing_approval_requests: {template.existing_approval_request_count}")
        print(f"recommended_next_step: {template.recommended_next_step}")
        print(
            render_expansion_operator_approval_schema_migration_selection_input_template_line(
                template
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-schema-migration-apply":
        AgentSystem(root).initialize()
        report_path, application = apply_operator_approval_schema_migration(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(f"expansion_operator_approval_schema_migration_apply: {application.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_template: {application.source_template_id}")
        print(f"source_status: {application.source_template_status}")
        print(f"source_packet: {application.source_packet_id}")
        print(f"source_checklist: {application.source_checklist_id}")
        print(f"source_ledger: {application.source_ledger_id}")
        print(f"source_request: {application.source_request_id}")
        print(f"source_plan: {application.source_plan_id}")
        print(f"source_decision: {application.source_decision_id}")
        print(f"source_review: {application.source_review_id}")
        print(f"target_table: {application.target_table}")
        print(f"operator_id: {application.operator_id}")
        print(f"selected_action: {application.selected_action}")
        print(f"inputs_recorded: {application.inputs_recorded_count}")
        print(f"missing_required_inputs: {application.missing_required_input_count}")
        print(f"actions_taken: {application.actions_taken_count}")
        print(f"migration_applied: {application.migration_applied_count}")
        print(f"table_created: {application.table_created_count}")
        print(
            "operator_approval_rows_created: "
            f"{application.operator_approval_row_count}"
        )
        print(f"approval_requests_created: {application.created_approval_request_count}")
        print(f"existing_approval_requests: {application.existing_approval_request_count}")
        print(
            render_operator_approval_schema_migration_application_line(
                application
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-request-rows-apply":
        AgentSystem(root).initialize()
        report_path, application = apply_operator_approval_request_rows(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(f"expansion_operator_approval_request_rows_apply: {application.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_draft: {application.source_draft_id}")
        print(f"source_status: {application.source_draft_status}")
        print(f"source_schema_application: {application.source_schema_application_id}")
        print(
            "source_schema_status: "
            f"{application.source_schema_application_status}"
        )
        print(f"source_ledger: {application.source_ledger_id}")
        print(f"source_checklist: {application.source_checklist_id}")
        print(f"source_index: {application.source_index_id}")
        print(f"source_brief: {application.source_brief_id}")
        print(f"source_audit: {application.source_audit_id}")
        print(f"operator_id: {application.operator_id}")
        print(f"selected_action: {application.selected_action}")
        print(f"draft_requests: {application.draft_request_count}")
        print(
            "operator_approval_rows_created: "
            f"{application.operator_approval_row_count}"
        )
        print(f"approval_requests_created: {application.created_approval_request_count}")
        print(
            "existing_operator_approval_requests: "
            f"{application.existing_operator_approval_request_count}"
        )
        print(f"external_requests: {application.external_request_count}")
        print(f"capability_requests: {application.capability_request_count}")
        print(
            render_operator_approval_request_rows_application_line(
                application
            ).removeprefix("- ")
        )
        return 0

    if args.command == "expansion-operator-approval-request-decide":
        AgentSystem(root).initialize()
        report_path, decision = decide_operator_approval_requests(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(f"expansion_operator_approval_request_decide: {decision.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_row_application: {decision.source_row_application_id}")
        print(f"source_status: {decision.source_row_application_status}")
        print(f"source_draft: {decision.source_draft_id}")
        print(f"source_schema_application: {decision.source_schema_application_id}")
        print(f"source_ledger: {decision.source_ledger_id}")
        print(f"source_checklist: {decision.source_checklist_id}")
        print(f"source_index: {decision.source_index_id}")
        print(f"source_brief: {decision.source_brief_id}")
        print(f"source_audit: {decision.source_audit_id}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"pending_requests_before: {decision.pending_request_count_before}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(f"approved_decisions: {decision.approved_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"pending_requests_after: {decision.pending_request_count_after}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"external_requests: {decision.external_request_count}")
        print(f"capability_requests: {decision.capability_request_count}")
        print(
            render_operator_approval_request_decision_line(decision).removeprefix(
                "- "
            )
        )
        return 0

    if args.command == "expansion-operator-approval-effect-proposals":
        AgentSystem(root).initialize()
        report_path, summary, effects = write_operator_approval_effect_proposals(root)
        print(f"expansion_operator_approval_effect_proposals: {summary.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_decision: {summary.source_decision_id}")
        print(f"source_draft: {summary.source_draft_id}")
        print(
            "approved_operator_requests: "
            f"{summary.approved_operator_request_count}"
        )
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(f"external_effect_proposals: {summary.external_effect_proposal_count}")
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(
            "legacy_approval_requests_created: "
            f"{summary.legacy_approval_request_count}"
        )
        print(f"activation_actions_taken: {summary.activation_action_count}")
        for effect in effects:
            print(render_operator_approval_effect_proposal_line(effect).removeprefix("- "))
        return 0

    if args.command == "expansion-operator-approval-effect-apply":
        AgentSystem(root).initialize()
        report_path, application, effects = apply_operator_approval_effects(
            root,
            operator_id=args.operator_id,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(f"expansion_operator_approval_effect_apply: {application.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {application.operator_id}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(f"existing_applied_effects: {application.existing_applied_effect_count}")
        print(f"external_effects_applied: {application.external_effect_count}")
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(
            "legacy_approval_requests_created: "
            f"{application.legacy_approval_request_count}"
        )
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(
            render_operator_approval_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(render_operator_approval_effect_proposal_line(effect).removeprefix("- "))
        return 0

    if args.command == "capability-activation-tasks":
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, _existing_tasks, _applied_effects = (
            write_capability_activation_tasks(root)
        )
        print(f"capability_activation_tasks: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_application: {batch.source_application_id}")
        print(f"goal: {batch.goal_id}")
        print(f"applied_capability_effects: {batch.applied_capability_effect_count}")
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_activation_tasks: {batch.existing_task_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_task_batch_line(batch).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"status={task.status} source_effect={task.evidence.get('source_effect_id')}"
            )
        return 0

    if args.command == "capability-activation-contracts":
        AgentSystem(root).initialize()
        report_path, batch, created_contracts, _existing_contracts, _activation_tasks = (
            write_capability_activation_contracts(root)
        )
        print(f"capability_activation_contracts: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_task_batch: {batch.source_task_batch_id}")
        print(f"activation_tasks: {batch.activation_task_count}")
        print(f"contracts_created: {batch.contract_count}")
        print(f"existing_contracts: {batch.existing_contract_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_contract_batch_line(batch).removeprefix("- ")
        )
        for contract in created_contracts:
            print(
                f"contract={contract.id} capability={contract.capability} "
                f"status={contract.status} task={contract.task_id} "
                f"approval_boundary={contract.approval_boundary}"
            )
        return 0

    if args.command == "capability-activation-evidence":
        AgentSystem(root).initialize()
        report_path, batch, created_records, _existing_records, _contracts = (
            record_capability_activation_evidence(
                root,
                contract_id=args.contract_id,
                all_contracts=args.all,
                evidence_kind=args.evidence_kind,
                evidence_reference=args.evidence_reference,
                verification_command=args.verification_command,
                verification_status=args.verification_status,
                recorded_by=args.recorded_by,
                summary=args.summary,
            )
        )
        print(f"capability_activation_evidence: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"contracts_selected: {batch.contract_count}")
        print(f"evidence_records_created: {batch.evidence_record_count}")
        print(f"existing_evidence_records: {batch.existing_evidence_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(render_capability_activation_evidence_batch_line(batch).removeprefix("- "))
        for record in created_records:
            print(
                f"evidence={record.id} capability={record.capability} "
                f"contract={record.contract_id} verification_status={record.verification_status}"
            )
        return 0

    if args.command == "capability-activation-decide":
        AgentSystem(root).initialize()
        report_path, decision, _decided_contracts = decide_capability_activation_contracts(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(f"capability_activation_decide: {decision.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"contracts_ready: {decision.contract_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(f"approved_decisions: {decision.approved_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(render_capability_activation_decision_line(decision).removeprefix("- "))
        return 0

    if args.command == "capability-activation-followups":
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, _existing_tasks, _contracts = (
            write_capability_activation_followups(root)
        )
        print(f"capability_activation_followups: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"source_decision: {batch.source_decision_id}")
        print(f"contracts_selected: {batch.contract_count}")
        print(f"followup_tasks_created: {batch.followup_task_count}")
        print(f"existing_followup_tasks: {batch.existing_followup_task_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_batch_line(batch).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"contract={task.evidence.get('source_contract_id')} "
                f"status={task.status}"
            )
        return 0

    if args.command == "capability-activation-followup-delegations":
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, _existing_delegations, _tasks = (
            write_capability_activation_followup_delegations(root)
        )
        print(f"capability_activation_followup_delegations: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"followup_tasks: {batch.followup_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} category={delegation.category} "
                f"status={delegation.status}"
            )
        return 0

    if args.command == "capability-activation-followup-results":
        AgentSystem(root).initialize()
        report_path, batch, created_records, _existing_records, _delegations = (
            write_capability_activation_followup_results(root)
        )
        print(f"capability_activation_followup_results: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.followup_task_id} capability={record.capability} "
                f"status={record.evidence_status}"
            )
        return 0

    if args.command == "capability-activation-followup-result-decide":
        AgentSystem(root).initialize()
        report_path, decision, decided_records = (
            decide_capability_activation_followup_results(
                root,
                operator_id=args.operator_id,
                selected_action=args.selected_action,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(f"capability_activation_followup_result_decide: {decision.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(
            render_capability_activation_followup_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.followup_task_id} capability={record.capability} "
                f"status={record.evidence_status}"
            )
        return 0

    if args.command == "capability-activation-followup-result-effect-proposals":
        AgentSystem(root).initialize()
        report_path, summary, effects = (
            write_capability_activation_followup_result_effect_proposals(root)
        )
        print(
            "capability_activation_followup_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in effects:
            print(
                render_capability_activation_followup_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if args.command == "capability-activation-followup-result-effect-apply":
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {application.operator_id}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(f"existing_applied_effects: {application.existing_applied_effect_count}")
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if args.command == "capability-activation-followup-result-tasks":
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, _existing_tasks, _effects = (
            write_capability_activation_followup_result_tasks(root)
        )
        print(f"capability_activation_followup_result_tasks: {batch.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"applied_followup_effects: {batch.applied_followup_effect_count}")
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"result={task.evidence.get('source_result_id')} "
                f"status={task.status}"
            )
        return 0

    if args.command == "capability-activation-followup-result-task-delegations":
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, _existing_delegations, _tasks = (
            write_capability_activation_followup_result_task_delegations(root)
        )
        print(
            "capability_activation_followup_result_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} category={delegation.category} "
                f"status={delegation.status}"
            )
        return 0

    if args.command == "capability-activation-followup-result-task-results":
        AgentSystem(root).initialize()
        report_path, batch, created_records, _existing_records, _delegations = (
            write_capability_activation_followup_result_task_results(root)
        )
        print(
            "capability_activation_followup_result_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability} "
                f"status={record.evidence_status}"
            )
        return 0

    if args.command == "capability-activation-followup-result-task-result-decide":
        AgentSystem(root).initialize()
        report_path, decision, ready_records = (
            decide_capability_activation_followup_result_task_results(
                root,
                operator_id=args.operator_id,
                selected_action=args.selected_action,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(
            "approval_requests_created: "
            f"{decision.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in ready_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability} "
                f"status={record.evidence_status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        report_path, summary, effects = (
            write_capability_activation_followup_result_task_result_effect_proposals(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {application.operator_id}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(f"existing_applied_effects: {application.existing_applied_effect_count}")
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if args.command == "capability-activation-followup-result-task-result-effect-tasks":
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, _existing_tasks, _effects = (
            write_capability_activation_followup_result_task_result_effect_tasks(root)
        )
        print(
            "capability_activation_followup_result_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"applied_downstream_effects: {batch.applied_downstream_effect_count}")
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"result={task.evidence.get('source_downstream_result_id')} "
                f"status={task.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, _existing_delegations, _tasks = (
            write_capability_activation_followup_result_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} category={delegation.category} "
                f"status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            _existing_records,
            missing_result_artifact_delegations,
            _delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(
            "missing_result_artifacts: "
            f"{len(missing_result_artifact_delegations)}"
        )
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} "
                f"capability={record.capability} status={record.evidence_status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        report_path, decision, ready_records = (
            decide_capability_activation_followup_result_task_result_effect_task_results(
                root,
                operator_id=args.operator_id,
                selected_action=args.selected_action,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(
            "approval_requests_created: "
            f"{decision.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in ready_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability} "
                f"status={record.evidence_status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        report_path, summary, effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_proposals(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"source_effect={task.evidence.get('source_effect_id')} "
                f"status={task.status}"
            )
        for task in existing_tasks:
            print(
                f"existing_task={task.id} capability={task.evidence.get('capability')} "
                f"source_effect={task.evidence.get('source_effect_id')} "
                f"status={task.status}"
            )
        for effect in applied_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(f"delegation={delegation.id} task={delegation.parent_task_id}")
        for delegation in existing_delegations:
            print(
                f"existing_delegation={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        for task in downstream_tasks:
            print(
                f"downstream_task={task.id} capability={task.evidence.get('capability')} "
                f"source_effect={task.evidence.get('source_effect_id')} "
                f"status={task.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifact_delegations,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(
            f"missing_result_artifacts: {len(missing_result_artifact_delegations)}"
        )
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        for delegation in missing_result_artifact_delegations:
            print(
                f"missing_result_artifact={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            summary,
            created_effects,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposals(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(
            "existing_effect_proposals: "
            f"{summary.existing_effect_proposal_count}"
        )
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in created_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"source_effect={task.evidence.get('source_effect_id')} "
                f"status={task.status}"
            )
        for task in existing_tasks:
            print(
                f"existing_task={task.id} capability={task.evidence.get('capability')} "
                f"source_effect={task.evidence.get('source_effect_id')} "
                f"status={task.status}"
            )
        for effect in applied_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(f"delegation={delegation.id} task={delegation.parent_task_id}")
        for delegation in existing_delegations:
            print(
                f"existing_delegation={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        for task in downstream_tasks:
            print(
                f"downstream_task={task.id} capability={task.evidence.get('capability')} "
                f"source_effect={task.evidence.get('source_effect_id')} "
                f"status={task.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifact_delegations,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(
            f"missing_result_artifacts: {len(missing_result_artifact_delegations)}"
        )
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        for delegation in missing_result_artifact_delegations:
            print(
                f"missing_result_artifact={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            summary,
            created_effects,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(
            "existing_effect_proposals: "
            f"{summary.existing_effect_proposal_count}"
        )
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in created_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"status={task.status} risk={task.risk_level}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} "
                f"category={delegation.category} status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifact_delegations,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(
            f"missing_result_artifacts: {len(missing_result_artifact_delegations)}"
        )
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        for delegation in missing_result_artifact_delegations:
            print(
                f"missing_result_artifact={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} "
                f"task={delegation.parent_task_id}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            summary,
            created_effects,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(
            "existing_effect_proposals: "
            f"{summary.existing_effect_proposal_count}"
        )
        print(
            "capability_effect_proposals: "
            f"{summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in created_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"status={task.status} risk={task.risk_level}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} "
                f"category={delegation.category} status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifact_delegations,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(
            "missing_result_artifacts: "
            f"{len(missing_result_artifact_delegations)}"
        )
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"created_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} "
                f"capability={record.capability} status={record.evidence_status}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} "
                f"capability={record.capability} status={record.evidence_status}"
            )
        for delegation in missing_result_artifact_delegations:
            print(
                f"missing_result_artifact={delegation.id} "
                f"task={delegation.parent_task_id} "
                f"artifact={delegation.result_artifact_path}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} "
                f"task={delegation.parent_task_id} status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            summary,
            created_effects,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(
            f"capability_effect_proposals: {summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in created_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {application.operator_id}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"status={task.status} risk={task.risk_level}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifacts,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(f"missing_result_artifacts: {len(missing_result_artifacts)}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} status={record.evidence_status}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} status={record.evidence_status}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} task={delegation.parent_task_id} "
                f"status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} status={delegation.status}"
            )
        return 0


    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {application.operator_id}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"status={task.status} risk={task.risk_level}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-delegations"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_delegations, existing_delegations, downstream_tasks = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegations: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"downstream_tasks: {batch.downstream_task_count}")
        print(f"routing_decisions_created: {batch.routing_decision_count}")
        print(f"delegations_created: {batch.delegation_count}")
        print(f"existing_delegations: {batch.existing_delegation_count}")
        print(f"execution_started: {batch.execution_started_count}")
        print(f"network_actions_taken: {batch.network_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_delegation_batch_line(
                batch
            ).removeprefix("- ")
        )
        for delegation in created_delegations:
            print(
                f"delegation={delegation.id} task={delegation.parent_task_id} "
                f"profile={delegation.assigned_profile} status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifacts,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(f"missing_result_artifacts: {len(missing_result_artifacts)}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} status={record.evidence_status}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} status={record.evidence_status}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} task={delegation.parent_task_id} "
                f"status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-results"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            batch,
            created_records,
            existing_records,
            missing_result_artifacts,
            completed_delegations,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"completed_delegations: {batch.completed_delegation_count}")
        print(f"result_records_created: {batch.result_record_count}")
        print(f"existing_result_records: {batch.existing_result_record_count}")
        print(f"missing_result_artifacts: {len(missing_result_artifacts)}")
        print(f"approval_requests_created: {batch.created_approval_request_count}")
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_batch_line(
                batch
            ).removeprefix("- ")
        )
        for record in created_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} status={record.evidence_status}"
            )
        for record in existing_records:
            print(
                f"existing_result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} status={record.evidence_status}"
            )
        for delegation in completed_delegations:
            print(
                f"completed_delegation={delegation.id} task={delegation.parent_task_id} "
                f"status={delegation.status}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        report_path, summary, created_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(
            f"capability_effect_proposals: {summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in created_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-apply"
    ):
        AgentSystem(root).initialize()
        report_path, application, effects = (
            apply_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effects(
                root,
                operator_id=args.operator_id,
                selection_note=args.selection_note,
                evidence_reference=args.evidence_reference,
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_apply: "
            f"{application.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {application.operator_id}")
        print(f"proposed_effects: {application.proposed_effect_count}")
        print(f"effects_applied: {application.applied_effect_count}")
        print(
            "existing_applied_effects: "
            f"{application.existing_applied_effect_count}"
        )
        print(f"capability_effects_applied: {application.capability_effect_count}")
        print(f"approval_requests_created: {application.approval_request_count}")
        print(f"activation_actions_taken: {application.activation_action_count}")
        print(f"external_mutations_taken: {application.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_application_line(
                application
            ).removeprefix("- ")
        )
        for effect in effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-tasks"
    ):
        AgentSystem(root).initialize()
        report_path, batch, created_tasks, existing_tasks, applied_effects = (
            write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks(
                root
            )
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_tasks: "
            f"{batch.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(
            "applied_downstream_effects: "
            f"{batch.applied_downstream_effect_count}"
        )
        print(f"tasks_created: {batch.task_count}")
        print(f"existing_downstream_tasks: {batch.existing_task_count}")
        print(f"capability_tasks_created: {batch.capability_task_count}")
        print(
            "approval_requests_created: "
            f"{batch.created_approval_request_count}"
        )
        print(f"activation_actions_taken: {batch.activation_action_count}")
        print(f"external_mutations_taken: {batch.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_batch_line(
                batch
            ).removeprefix("- ")
        )
        for task in created_tasks:
            print(
                f"task={task.id} capability={task.evidence.get('capability')} "
                f"effect={task.evidence.get('source_effect_id')} "
                f"status={task.status} risk={task.risk_level}"
            )
        return 0


    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-decide"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            decision,
            decided_records,
        ) = decide_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_results(
            root,
            operator_id=args.operator_id,
            selected_action=args.selected_action,
            selection_note=args.selection_note,
            evidence_reference=args.evidence_reference,
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decide: "
            f"{decision.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"operator_id: {decision.operator_id}")
        print(f"selected_action: {decision.selected_action}")
        print(f"results_ready: {decision.result_record_count}")
        print(f"decisions_recorded: {decision.decision_count}")
        print(
            "accepted_keep_blocked_decisions: "
            f"{decision.accepted_keep_blocked_decision_count}"
        )
        print(f"more_evidence_decisions: {decision.more_evidence_decision_count}")
        print(f"deferred_decisions: {decision.deferred_decision_count}")
        print(f"existing_decisions: {decision.existing_decision_count}")
        print(f"approval_requests_created: {decision.created_approval_request_count}")
        print(f"activation_actions_taken: {decision.activation_action_count}")
        print(f"external_mutations_taken: {decision.external_mutation_count}")
        print(
            render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_decision_line(
                decision
            ).removeprefix("- ")
        )
        for record in decided_records:
            print(
                f"result={record.id} delegation={record.delegation_id} "
                f"task={record.downstream_task_id} capability={record.capability}"
            )
        return 0

    if (
        args.command
        == "capability-activation-followup-result-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-task-result-effect-proposals"
    ):
        AgentSystem(root).initialize()
        (
            report_path,
            summary,
            created_effects,
        ) = write_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals(
            root
        )
        print(
            "capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposals: "
            f"{summary.status}"
        )
        print(f"report: {report_path.relative_to(root)}")
        print(f"accepted_decisions: {summary.accepted_decision_count}")
        print(f"accepted_results: {summary.accepted_result_count}")
        print(f"effect_proposals_created: {summary.effect_proposal_count}")
        print(f"existing_effect_proposals: {summary.existing_effect_proposal_count}")
        print(
            f"capability_effect_proposals: {summary.capability_effect_proposal_count}"
        )
        print(f"approval_requests_created: {summary.approval_request_count}")
        print(f"activation_actions_taken: {summary.activation_action_count}")
        print(f"external_mutations_taken: {summary.external_mutation_count}")
        for effect in created_effects:
            print(
                render_capability_activation_followup_result_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_task_result_effect_proposal_line(
                    effect
                ).removeprefix("- ")
            )
        return 0

    if args.command == "approve":
        system = AgentSystem(root)
        system.initialize()
        approval = system.storage.approve_approval_request(
            args.approval_id,
            decided_by=args.decided_by,
            decision_note=args.note,
        )
        print(f"approved: {approval.id}")
        print(f"task: {approval.task_id}")
        return 0

    if args.command == "commit-approved":
        try:
            result = commit_approved_effect(
                root,
                approval_id=args.approval_id,
                committed_by=args.committed_by,
                message=args.message,
            )
        except (KeyError, ValueError) as error:
            print(f"commit_approved_failed: {error}")
            return 1
        print(f"commit_approved: {result.status}")
        print(f"effect_id: {result.effect.id}")
        print(f"approval_id: {result.approval_id}")
        print(f"commit: {result.commit_sha or ''}")
        print(f"worktree: {result.worktree_path}")
        print(f"evidence: {result.evidence_path}")
        return 0

    if args.command == "cleanup-worktrees":
        try:
            result = cleanup_worktrees(
                root,
                confirm=args.confirm,
                decided_by=args.decided_by,
                reason=args.reason,
            )
        except (KeyError, RuntimeError, ValueError) as error:
            print(f"worktree_cleanup_failed: {error}")
            return 1
        print(
            f"worktree_cleanup: {result.status} "
            f"eligible={result.eligible_count} "
            f"removed={result.removed_count} "
            f"blocked={result.blocked_count} "
            f"already_removed={result.already_removed_count} "
            f"skipped={result.skipped_count}"
        )
        for record in result.records:
            print(
                f"{record.id}: status={record.status} effect={record.effect_id} "
                f"reason={record.cleanup_reason} worktree={record.worktree_path} "
                f"evidence={record.evidence_path}"
            )
        return 1 if result.blocked_count else 0

    if args.command == "github-handoff":
        try:
            result = create_github_handoff(
                root,
                effect_id=args.effect_id,
                remote=args.remote,
                base=args.base,
                title=args.title,
            )
        except (KeyError, RuntimeError, ValueError) as error:
            print(f"github_handoff_failed: {error}")
            return 1
        handoff = result.handoff
        print(f"github_handoff: {result.status}")
        print(f"handoff_id: {handoff.id}")
        print(f"effect_id: {handoff.effect_id}")
        print(f"commit: {handoff.commit_sha}")
        print(f"branch: {handoff.branch_name}")
        print(f"remote: {handoff.remote_name} {handoff.remote_url}")
        print(f"push_command: {handoff.push_command}")
        print(f"draft_pr_command: {handoff.draft_pr_command}")
        print(f"evidence: {handoff.evidence_path}")
        return 0

    if args.command == "ci-deploy-evidence":
        try:
            result = record_ci_deploy_evidence(
                root,
                github_handoff_id=args.github_handoff_id,
                provider=args.provider,
                status=args.status,
                external_run_id=args.external_run_id,
                external_url=args.url,
                recorded_by=args.recorded_by,
                note=args.note,
            )
        except (KeyError, ValueError) as error:
            print(f"ci_deploy_evidence_failed: {error}")
            return 1
        record = result.record
        print(f"ci_deploy_evidence: {result.status}")
        print(f"record_id: {record.id}")
        print(f"github_handoff_id: {record.github_handoff_id}")
        print(f"effect_id: {record.effect_id}")
        print(f"commit: {record.commit_sha}")
        print(f"branch: {record.branch_name}")
        print(f"provider: {record.provider}")
        print(f"status: {record.status}")
        print(f"external_run_id: {record.external_run_id}")
        print(f"external_url: {record.external_url}")
        print(f"network_actions_taken: {record.result_json['network_actions_taken']}")
        print(f"evidence: {record.evidence_path}")
        return 0

    if args.command == "resolve-incident":
        resolved = AgentSystem(root).resolve_incident(
            args.incident_id,
            resolved_by=args.resolved_by,
            resolution_note=args.note,
        )
        print(f"resolved_incident: {resolved.id}")
        print(f"status: {resolved.status}")
        print(f"resolution_evidence: {resolved.resolution_evidence_path}")
        return 0

    if args.command == "sweep-stuck":
        incident_ids = AgentSystem(root).detect_stuck_tasks(
            timeout_seconds=args.timeout_seconds,
            now=args.now,
        )
        print(f"stuck_incidents: {len(incident_ids)}")
        for incident_id in incident_ids:
            print(incident_id)
        return 0

    if args.command == "queue-health":
        AgentSystem(root).initialize()
        report_path, findings = write_queue_health_report(
            root,
            blocked_threshold=args.blocked_threshold,
            failed_threshold=args.failed_threshold,
        )
        print(f"queue_health: {report_path.relative_to(root)}")
        print(f"hotspots: {len(findings)}")
        for finding in findings:
            print(render_queue_health_finding(finding).removeprefix("- "))
        return 0

    if args.command == "playbooks":
        playbooks = promote_successful_run_playbooks(
            root,
            min_successes=args.min_successes,
        )
        print(f"playbooks: {len(playbooks)}")
        for playbook in playbooks:
            print(render_playbook_line(playbook).removeprefix("- "))
        return 0

    if args.command == "eval-after-change":
        report_path, check = run_eval_after_change(
            root,
            change_summary=args.change,
            changed_paths=args.file,
        )
        print(f"eval_after_change: {check.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"evals: {','.join(check.eval_names)}")
        print(f"run_ids: {','.join(check.run_ids)}")
        print(f"result_paths: {','.join(check.result_paths)}")
        return 0 if check.status == "pass" else 1

    if args.command == "distill-learnings":
        report_path, distillation = distill_learnings(
            root,
            min_occurrences=args.min_occurrences,
        )
        print(f"learning_distillation: {distillation.status}")
        print(f"report: {report_path.relative_to(root)}")
        print(f"stable_learnings: {distillation.stable_learning_count}")
        print(f"source_learnings: {distillation.source_learning_count}")
        for learning in distillation.stable_learnings:
            print(render_stable_learning_line(learning).removeprefix("- "))
        return 0

    if args.command == "eval-candidates":
        system = AgentSystem(root)
        system.initialize()
        candidates = system.storage.list_recent_eval_candidates()
        print(f"eval_candidates: {len(candidates)}")
        for candidate in candidates:
            print(render_eval_candidate_line(candidate))
        return 0

    if args.command == "capabilities-json":
        capabilities = [
            capability.__dict__ for capability in detect_runtime_capabilities(root)
        ]
        print(json.dumps(capabilities, indent=2, sort_keys=True))
        return 0

    if args.command == "run-goal":
        if args.isolation == "worktree":
            if not args.exec_command:
                print("coding_run_failed: --command is required for worktree isolation")
                return 1
            try:
                result = run_worktree_coding_goal(
                    root,
                    project_name=args.project,
                    description=args.description,
                    command=args.exec_command,
                )
            except ValueError as error:
                print(f"coding_run_failed: {error}")
                return 1
            print(f"coding_run: {result.status}")
            print(f"goal_id: {result.goal_id}")
            print(f"run_id: {result.run_id}")
            print(f"task_id: {result.task_id}")
            print(f"worktree: {result.worktree.worktree_path}")
            print(f"branch: {result.worktree.branch_name}")
            print(f"base_commit: {result.worktree.base_commit}")
            print(f"effect_id: {result.effect.id}")
            print(f"effect_type: {result.effect.effect_type}")
            print(f"effect_status: {result.effect.status}")
            print(f"approval_id: {result.approval_id}")
            print(f"evidence: {result.evidence_dir}")
            return 0 if result.status == "awaiting_approval" else 1
        result = AgentSystem(root).run_goal(args.project, args.description)
        print(f"status: {result.status}")
        print(f"goal_id: {result.goal_id}")
        print(f"run_id: {result.run_id}")
        print(f"activity: {result.activity_path}")
        print(f"summary: {result.summary_path}")
        print(f"learning: {result.learning_path}")
        print(f"eval_candidate: {result.eval_candidate_path}")
        return 0 if result.status == "completed" else 1

    if args.command == "eval":
        if args.name != "first_milestone_closed_loop":
            parser.error(f"unknown eval: {args.name}")
        result = run_first_milestone_eval(root)
        print(f"{result.name}: {result.status}")
        print(f"result: {result.result_path}")
        return 0 if result.status == "pass" else 1

    parser.error(f"unknown command: {args.command}")
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
